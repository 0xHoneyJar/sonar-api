# Claims to Verify

> Generated by `/ride --enriched --force-restore` on 2026-07-19.
> Interview skipped — derived from existing `grimoires/loa/context/` + active PRD/SDD headers.
> Session hash: `4d9f`

## Covered topics (interview skip)

- 6-chain EVM belt + Solana SVM deep history
- Self-hosted Envio HyperIndex + Hasura + gateway promote path
- SQD Portal as SVM genesis lake (Envio HyperSync-Solana eliminated for deep history)
- Warehouse (Dune) vs Helius two-lane SVM architecture
- Zero-downtime blue/green belt promotion
- Canonical NftActivity + Token ownership index (active planning PRD)

## Architecture Claims

| Claim | Source | Verification strategy |
|-------|--------|----------------------|
| Indexer name is `thj-indexer` / package `envio-indexer` | README; config.yaml | Confirm `config.yaml` `name:` and `package.json:name` |
| 6 EVM chains: 1, 10, 8453, 42161, 7777777, 80094 | README; context lake ADR | Grep `config.yaml` network `id:` blocks |
| Runtime Node ≥22 / pnpm | README badge; package.json | Compare `engines.node` vs `.cursor/rules/hyperindex.mdc` (v20 claim) |
| Framework is Envio HyperIndex 3.x | README; package.json | Confirm `dependencies.envio` version |
| Green belt entry is `src/EventHandlers.ts` side-effect imports | EventHandlers.ts header | Confirm config.yaml `handler:` paths + import list |
| Kitchen HTTP service onboards collections on demand | README; src/kitchen/ | Confirm Hono routes + Dockerfile.kitchen |
| Promote path is `scripts/promote.sh` with non-skippable gate | NOTES; scripts/promote.sh | Confirm gate precondition + BELT_UPSTREAM write |
| GraphQL served via Hasura behind gateway alias | README | Confirm Dockerfile.gateway + Caddyfile presence |

## Domain Claims

| Claim | Source | Verification strategy |
|-------|--------|----------------------|
| Generic ERC-721 lane uses TrackedHolder / Action overlays | PRD; handlers/tracked-erc721 | Confirm entities in schema.graphql + handler emits |
| Token entity exists for ownership index | schema.graphql:350; PRD #153 | Confirm Token fields + handlers writing Token |
| NftActivity canonical layer in src/canonical/ | PRD; canonical/* | Confirm map-evm/map-svm/emit/parity + @0xhoneyjar/events |
| TrackedTokenBalance tracks ERC-20 balances | tracked-erc20.ts | Confirm entity + set/get usage |
| SVM history uses SQD Portal from block 0 | lake-decision-record; sqd-* | Confirm sqd-client/loader + GATE verdict in context |
| Helius is live-tail / enrichment lane, not history substrate | warehouse ADR 2026-07-05 | Confirm helius vs warehouse-loader roles in src/svm/ |
| Warehouse lane uses Dune tokens_solana.* | warehouse ADR | Confirm src/svm/sql/ + warehouse-loader.ts |
| §4.5 gate is reconcile-by-recompute trust root | warehouse ADR; sqd-45-gate | Confirm test/scripts gate artifacts |

## Tribal Knowledge

| Claim | Source | Verification strategy |
|-------|--------|----------------------|
| KF-015: 6-chain needs NODE_OPTIONS heap bump | NOTES; Dockerfile.belt | Confirm loa:shortcut / NODE_OPTIONS in Dockerfile.belt |
| KF-020: self-host lakes free; hosting is the cost | README | Treat as operational doctrine; not code-falsifiable |
| ENVIO_PG_SSL_MODE=false required (KF-013) | NOTES | Search env usage / runbooks |
| Ponder paths are legacy; Envio is live | beads bd-c7jv; CI names | Confirm workflows + absence of live ponder schema path |

## WIP Status

| Claim | Source | Verification strategy |
|-------|--------|----------------------|
| EVM Collection Onboarding Contract v1 is active planning SoT | prd.md header 2026-07-07 | Do not overwrite prd.md/sdd.md in this ride |
| Belt zero-downtime is sibling archived track | prd-belt-zero-downtime.md | Confirm sibling files exist; status from NOTES (swap live) |
| SQD parallel loader built for GATE-3 escape | lake-decision-record | Confirm `src/svm/sqd-parallel-loader.ts` exists |
| Pythians 30k fixture historically blocked gates | NOTES 2026-07-06 | Confirm fixture presence under test/fixtures |
