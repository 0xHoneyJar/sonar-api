---
title: "Drift Report — freeside-sonar (Ponder-as-target reconciliation)"
trust_tier: operator-authored
read_state: unread
confidence: 0.8
decay_class: working
last_confirmed: 2026-06-29
operator_signed: self_attested
---

# Drift Report — freeside-sonar (Post-Promote Continuity Alignment)

> Generated by `/ride` (riding-codebase) · 2026-06-29 · Loa v1.157.0
> Trigger: **post-promote continuity alignment** after PR #101 merged to `main`
> (`87705e43`, branch `feat/envio-cloud-hypersync`: canonical event normalization
> + SVM events + labeling attribution registry).
> Supersedes the 2026-05-19 drift-report (which predates the entire Ponder episode
> and reported the codebase "healthy, 8.5/10" with zero awareness of `ponder-runtime/`).

## Operator reality correction (the anchor for this ride)

> **sonar MIGRATED BACK FROM PONDER TO ENVIO.** Envio — **self-hosted on Railway,
> NOT managed Envio Cloud** — is the canonical/live runtime. `ponder-runtime/` and
> the `ponder@0.16.6` dependency are **vestigial / superseded**, not the target.

Every finding below is measured against that corrected ground truth. The dominant
drift class in this repo is **inverted runtime authority**: code-adjacent docs, CI
workflows, and the parked planning grimoire still assert *Ponder is production /
Envio is retired* — the exact inverse of current reality.

## Code-truth re-extraction (what actually runs)

| Surface | Evidence | Status |
|---------|----------|--------|
| **Live runtime = Envio HyperIndex** | `package.json:2` `name: "envio-indexer"`; scripts `codegen/dev/start` = `envio …` (`package.json:9-11`); `Dockerfile.belt:1` "Envio HyperIndex belt indexer service for Railway"; `Dockerfile.belt:53` `CMD … pnpm envio start --config "$BELT_CONFIG"` | **CANONICAL** |
| **Live manifest** | `config.yaml:11` `name: thj-indexer`; 6 EVM chains (1·10·42161·7777777·8453·80094) `config.yaml:555-673`; 41 contract defs | **CANONICAL** |
| **Envio version** | `Dockerfile.belt:14` `envio@3.0.0-alpha.17` (managed-Cloud attempt went to `3.2.1` per `git 9cf7152f`) | live |
| **Ponder runtime** | `ponder-runtime/` (~40 handlers, `src/lib`, tests); root `ponder.config.ts`, `ponder.config.mibera.ts`, `ponder.schema.ts` (58 KB), `ponder-env.d.ts`; `Dockerfile.belt-ponder:1-3` "Replaces the envio Dockerfile.belt for the migration to Ponder"; `package.json:43` `ponder: "0.16.6"` | **VESTIGIAL** |
| **PR #101 new subsystems** | `src/canonical/` (chain-agnostic NftActivity normalizer: emit/map-evm/map-svm/parity), `src/labels/` (L2 attribution registry: classify/ingest/pipeline/reconcile/signing/seed), `src/svm/` (Helius-based collection-event indexers) | live, **standalone peers** of the Envio belt — not wired into the EVM handler graph |
| src/ runtime lean | `src/` files mentioning `envio`: 37 · mentioning `ponder`: 1 | confirms Envio is the active surface |

**Drift Score: 4.5 / 10** (unhealthy — the *automated sensors themselves* (CI) and
the production-handler guidelines assert inverted authority). Not a code-defect
score; a documentation/governance-coherence score.

| Drift Type | Count | Max Severity |
|------------|-------|--------------|
| Stale-inverted (doc/CI asserts Ponder-as-production) | 5 | **Critical** |
| Stale (managed-Envio-Cloud framed as target) | 2 | High |
| Vestigial (dead-but-present Ponder surfaces) | 3 | Medium |
| Shadow / anomaly (code-truth gap) | 1 | Medium |
| Aligned (Envio-canonical evidence) | — | Healthy |

---

## CRITICAL — Inverted runtime authority (Ponder-as-target drift)

### PD-1: `ponder-ci.yml` declares itself "the PRODUCTION validation gate"
- **Doc**: `.github/workflows/ponder-ci.yml:1-19` — *"the PRODUCTION validation gate … envio was removed in the ponder migration … the code that actually runs the indexer in production — sensed by NOTHING"*; typechecks `ponder-runtime/tsconfig.json`, runs determinism lint.
- **Reality**: Envio is live (`Dockerfile.belt:53` `pnpm envio start`). `ponder-ci.yml` is now a green gate over **vestigial** code, asserting it is production. A passing `ponder-ci` run is a **numb-but-confident sensor** — green means the dead runtime typechecks.
- **Severity**: Critical (inverted production gate). **Flag, do not auto-fix** — CI/release-automation is outside micro-fix latitude; route through `/implement`.

### PD-2: `belt-build.yml` calls the Envio path "RETIRED"
- **Doc**: `.github/workflows/belt-build.yml` (Gate 2-3 comment) — *"the RETIRED envio belt (the `envio` binary was removed in the ponder migration … codegen can no longer run) … The LIVE production validation moved to ponder-ci.yml"*; Gates 2-3 `continue-on-error: true`.
- **Reality**: The Envio belt is live; `Dockerfile.belt` runs `envio codegen` + `envio start` in production. Gate 1 (`verify:belt-config`, config-fidelity) is still the meaningful blocking gate, but the workflow's own prose declares the runtime it guards "retired."
- **Severity**: Critical (inverted). The `continue-on-error` on the envio gates means a real envio codegen/typecheck regression would pass silently.

### PD-3: `ponder-runtime/GUIDELINES.md` — "the envio gates are retired"
- **Doc**: `ponder-runtime/GUIDELINES.md:48-52` — *"`ponder-ci.yml` … This is the production gate. Green means shippable. `belt-build.yml` — config-fidelity (Gate 1, live); **the envio gates are retired.**"* Opening line: *"Ponder re-executes handlers — on reorg, on restart …"*
- **Reality**: Ponder no longer runs in production. The determinism contract this file teaches applies to vestigial code.
- **Severity**: High.

### PD-4: `grimoires/loa/prd.md` — sovereign-Ponder framed as *current*, managed-Envio-Cloud as *target*
- **Doc** (parked **indexing-managed-envio** cycle, still sitting at the canonical `prd.md` path):
  - `prd.md:29` — *"move Layer-1 off **sovereign-Ponder-on-Railway** to **managed Envio**"*
  - `prd.md:48,52` — per-token ownership "lives in `ponder-runtime/`"; *"A newer **Ponder green-v3** carries the per-token + fuller-pillar work"*; *"Standing up Envio = measuring a lesser-featured indexer"*
  - `prd.md:120` — adopting managed Envio without re-porting the Ponder outbox = *"reliability regression"*
- **Reality**: **Doubly falsified.** (a) Ponder is not current — it was reverted to vestigial. (b) The target was **not** managed Envio Cloud — the cutover landed as **self-hosted Envio on Railway** (managed-Cloud attempt hit the KF-015 ~2 GB heap OOM @3.2.1 and was abandoned in favor of Railway self-host).
- **Severity**: High. The whole TCO-measurement frame (measure sovereign-Ponder vs managed-Envio) was settled by reverting to self-hosted Envio. A correction banner has been prepended to `prd.md` (this ride); the body is left intact (it is a parked cycle's record, not rewritten).

### PD-5: `grimoires/loa/sdd.md` — per-token logic + reliability model sourced from Ponder
- **Doc**: `sdd.md:269,274` per-token helper at `ponder-runtime/src/handlers/token-projection/shared.ts`; `sdd.md:505` *"Adopt the fuller Ponder `nats-publisher.ts` model (outbox + reorg-safe + DLQ)"*; `sdd.md:548` divergence "absent in the Envio handlers."
- **Reality**: Same inversion as PD-4. Design decisions keyed to "Ponder green-v3 is the future" are stale. Whether the per-token / outbox work was re-ported to the Envio surface is **unconfirmed** (see GAP-A below).
- **Severity**: High. Correction banner prepended (this ride).

---

## HIGH — managed-Envio-Cloud as target (the "Cloud" misnomer)

### PD-6: branch name + NOTES thread carry "Envio Cloud" framing
- **Doc**: merged branch `feat/envio-cloud-hypersync`; `git 9cf7152f` *"(managed-Envio Cloud target)"*; `git 7b0becda` KF-015 *"OOM on managed-Envio Cloud @3.2.1 (AC-PORT-9)"*; NOTES.md `AC-PORT-9` "Cloud gate" / resume pointer.
- **Reality**: Landed as **self-hosted Envio on Railway**, not managed Cloud. The "Cloud" in the branch/commit names is an abandoned aspiration. (Consistent with operator memory: *"Railway, not managed Cloud … the AC-PORT-9 Cloud gate is stale; KF-015 OOM is solved on Railway via NODE_OPTIONS."*)
- **Severity**: High (misleading provenance). The branch is already merged; no action beyond noting that "envio-cloud" ≡ "self-hosted Envio on Railway" for anyone reading the history.

---

## MEDIUM — Vestigial Ponder surfaces (dead-but-present)

### PD-7: `package.json` carries `ponder`, lacks `envio`
- `package.json:43` `ponder: "0.16.6"` (vestigial per operator). There is **no `envio` dependency** in `package.json` — the `envio` binary the live `Dockerfile.belt` invokes is sourced outside the manifest (git-sourced / image-global; consistent with memory *"envio is git-sourced & not installed locally"*). Net: the manifest advertises the dead runtime and omits the live one.
- **Action (flag)**: drop `ponder`, restore an explicit `envio` dep pin (`alpha.17`) — a dependency change, route through `/implement`, not a micro-fix.

### PD-8: root + `ponder-runtime/` Ponder files
- `ponder.config.ts`, `ponder.config.mibera.ts`, `ponder.schema.ts` (58 KB), `ponder-env.d.ts`, `Dockerfile.belt-ponder`, and the entire `ponder-runtime/` tree (~40 handlers + lib + 96 test files) are vestigial. They *look* load-bearing (recent mtimes from the merge touch; a dedicated CI gate) but run nothing in production.
- **Action (flag)**: operator decision — archive/delete vs keep as a re-port reference. Deletion is a structural change, not in-ride scope.

### PD-9: `ponder-ci.yml` runs `pnpm test` against the whole suite
- The "production" Ponder gate runs the 96-file vitest suite. Those tests retain value (byte-parity, holder math, address-type), but they are filed under a workflow that claims to validate production Ponder code that no longer ships.
- **Action (flag)**: re-home meaningful pure-logic tests under an Envio-labelled gate when the Ponder surface is retired.

---

## MEDIUM — Code-truth anomaly (honest-green gap)

### GAP-A: `config.yaml` references a handler file absent from HEAD
- **Code**: every contract in the 6-chain green manifest declares `handler: src/EventHandlers.ts` (`config.yaml`, all 41 contracts). **`src/EventHandlers.ts` does not exist in HEAD** (`git cat-file -e HEAD:src/EventHandlers.ts` → absent; last touched by `git 9cf7152f` #75, the managed-Envio-Cloud port). The only EventHandlers file present is the belt-scoped `src/belts/mibera/EventHandlers.mibera.ts` (used by `config.mibera.yaml`).
- **Interpretation (uncertain — flagged, not asserted)**: the live Railway deployment is `Dockerfile.belt` parameterized by `BELT_CONFIG`. The **Mibera belt** (`config.mibera.yaml` → `src/belts/mibera/EventHandlers.mibera.ts`) has an existing entry; the **6-chain green** (`config.yaml` → `src/EventHandlers.ts`) points at a deleted file. Either (a) the 6-chain green belt is not the active `BELT_CONFIG` and `config.yaml`'s handler ref is stale, or (b) `EventHandlers.ts` is reconstituted at build time. I could not confirm which from the working tree.
- **Action**: **operator confirm** which `BELT_CONFIG` the live `belt-indexer-selfhost` Railway service runs, and whether `config.yaml`'s `handler:` ref is stale. This blocks any claim that the 6-chain green belt is live-shippable from HEAD.

---

## Aligned (Envio-canonical — healthy signal)

- `Dockerfile.belt`, `package.json` identity/scripts, `config.yaml`, and the `src/` import-lean (37 envio : 1 ponder) all point one direction: **Envio is the live runtime.**
- PR #101's `src/canonical` + `src/labels` + `src/svm` are real, current work on the **Envio-era** surface (the SVM webhook is a deployed Railway service per `Dockerfile.svm-webhook`; backfill via `.github/workflows/svm-event-backfill.yml`). They are peer subsystems, not Ponder.
- The 2026-05-19 `reality/ride-2026-05-19-{prd,sdd}.md` describe **Envio HyperIndex V3** as the runtime — directionally **correct again** (only their version pins `alpha.14` are stale vs live `alpha.17`).

## Recommended actions (priority order — all flag-don't-fix in this ride)

1. **De-invert the CI sensors** (Critical): `ponder-ci.yml` + `belt-build.yml` prose + `ponder-runtime/GUIDELINES.md` assert Ponder-is-production. Route a `/implement` sprint to re-establish an Envio-labelled production gate and demote/retire the Ponder gate. Until then, a green `ponder-ci` is a confidently-wrong signal.
2. **Resolve GAP-A** (Medium-blocking): confirm live `BELT_CONFIG` and reconcile `config.yaml`'s missing `src/EventHandlers.ts` handler ref.
3. **Re-port verification** (High): confirm whether #69 per-token ownership + the fuller outbox/reorg-safe/DLQ NATS publisher were carried from `ponder-runtime/` back onto the Envio surface, or are lost in the revert (PD-5 / GAP-A).
4. **Vestigial cleanup decision** (Medium): archive/delete `ponder-runtime/`, root `ponder.*`, `Dockerfile.belt-ponder`, and the `ponder` dep — or keep as labelled re-port reference. Restore an explicit `envio` dep pin (PD-7).
5. **Parked-cycle docs**: `prd.md` / `sdd.md` now carry correction banners (this ride). When the indexing cycle is formally closed, move them to `grimoires/loa/cycles/indexing-managed-envio/` so they stop occupying the canonical path.
6. **NOTES.md AC-PORT-9**: mark the managed-Envio-Cloud "Cloud gate" resume pointer RESOLVED (self-host-on-Railway) so no future session re-attempts the managed-Cloud cutover.
