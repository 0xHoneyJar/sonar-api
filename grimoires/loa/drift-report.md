# Drift Report — freeside-sonar (THJ Envio Indexer)

> Generated by /ride · 2026-05-19 · Framework: Loa v1.157.0
> Supersedes the 2026-01-26 drift-report (which referenced Envio 2.x — now V3).

## Executive Summary

The indexer codebase is **healthy and well-aligned** with its documentation.
README, SCALE.md, and DEPLOYMENT_GUIDE accurately reflect code reality. The most
significant drift is **not** within the indexer — it is the Loa framework content
mixed into the repo root (see `reality/hygiene-report.md`).

| Drift Type | Count | Severity |
|------------|-------|----------|
| Ghost (documented, missing in code) | 0 | — |
| Stale (docs describe old code) | 4 | Medium |
| Hallucinated (docs claim unsupported things) | 0 | — |
| Shadow (code undocumented) | 2 | Low |
| Aligned | majority | Healthy |

**Drift Score: 8.5 / 10** (healthy). Penalized only for stale counts/versions in
reference docs and the framework-mixing hygiene issue.

## Stale (Docs Describe Outdated Code)

### ST-1: Envio version drift in older artifacts (Medium)
- **Doc**: prior `drift-report.md`, prior `claims-to-verify.md` — "Envio 2.32.2"
- **Code**: `package.json` → `envio@3.0.0-alpha.14`
- **Impact**: The codebase migrated to HyperIndex V3. Old grimoire docs predate it.
- **Resolution**: This ride's regenerated artifacts cite V3. Drift-resolution policy
  is `code` (per `.loa.config.yaml`) — docs updated to match code.

### ST-2: Node version requirement (Medium)
- **Doc**: prior claims — "Node v18+ / >=18.0.0"
- **Code**: `package.json` → `engines.node: ">=22.0.0"`; README correctly says v22+
- **Impact**: V3 requires Node 22. Old grimoire claims are stale; README is correct.

### ST-3: Entity / handler / contract counts (Low)
- **Doc**: `ENTITY_REFERENCE.md` — "88+ entities"; prior claims — "60+ handlers", "50+ contracts"
- **Code**: 93 entities (`schema.graphql`), 84 handlers, 41 contract definitions
- **Impact**: Counts grew/consolidated. Reference docs lag. Not user-facing.

### ST-4: `CLAUDE.md` thin on indexer specifics (Low)
- **Doc**: `CLAUDE.md` "Project Overview" — one sentence, no version/chain detail
- **Code**: Indexer is far richer (6 chains, V3, 41 contracts)
- **Impact**: AI guidance file underspecifies the project. Reality files now fill the gap.

## Shadow (Code Exists, Undocumented)

### SH-1: `seaport.ts` handler without active contract (Low)
- **Code**: `src/handlers/seaport.ts` implements `Seaport.OrderFulfilled.handler`
- **Doc gap**: Config entry for `Seaport` is commented out (`config.yaml`). No doc
  explains whether Seaport is staged-for-future or abandoned.
- **Action**: Maintainer decision — document intent or remove.

### SH-2: Alternate root configs undocumented (Low)
- **Code**: `config.sf-vaults.yaml`, `config.test-eventfilter.yaml`, `config.test-rebate.yaml`
- **Doc gap**: No doc explains these scratch/test configs.
- **Action**: Maintainer decision — document or relocate to a `configs/` dir.

## Ghosts / Hallucinations

**None found.** Every documented feature (CubQuests Action log, S&F vaults,
ApiologyDAO auctions, Mibera substrate, fatBERA staking, 6-chain support) is
backed by real handler code and schema entities.

## Aligned (Healthy)

- README's 6-chain claim, production endpoints, V3 stack — all verified.
- SCALE.md operator playbook matches the config-driven indexer model.
- `HANDLER_REGISTRY.md` structure (contract→handler→entity) matches `src/handlers/`.
- `recordAction` quest-log pattern matches the `Action` schema entity.

## Recommended Actions

1. Regenerate `ENTITY_REFERENCE.md` / `HANDLER_REGISTRY.md` with current counts (Low).
2. Decide Seaport handler fate (SH-1).
3. Address the framework-mixing hygiene finding (`hygiene-report.md`) — Critical for
   repo clarity, but a structural decision, not a code defect.
4. Expand `CLAUDE.md` Project Overview with the reality files' summary.
