# OKF/ICM Adoption Cycle — Grounding Brief (verified against current HEAD 1eeadff8, 2026-06-28)

## Audit-envelope verify_for_merge activation + trust-store bootstrap (claims re-verified against current HEAD, 4 days after last write)

### Claims verified
- [confirmed] Nothing in production calls audit_verify_chain(verify_for_merge=True) — the Python merge-gate is dormant (confirms #1103/#1104). — grep -rn verify_for_merge .claude/ → only 2 callers, both in test file: .claude/adapters/tests/test_audit_envelope_strict_verify.py:91,162. The python def is audit_envelope.py:535. grep across *.sh for verify_for_merge OR LOA_AUDIT_STRICT_VERIFY → ZERO matches. The env-var alternate trigger LOA_AUDIT_STRICT_VERIFY (audit_envelope.py:138) is set nowhere in config or scripts.
- [confirmed] Production uses a SEPARATE bash audit_verify_chain (audit-envelope.sh), and that bash path has NO verify_for_merge / strict mode at all. — Bash def audit-envelope.sh:758. Its gate _audit_check_trust_store (audit-envelope.sh:485-501) treats BOOTSTRAP-PENDING and VERIFIED identically (both return 0/permit); only INVALID blocks. There is no strict_verify parameter, no env override, no ATK-3 fail-closed branch in bash — unlike Python _check_trust_store (audit_envelope.py:470-490). The bash signature loop (lines 800-829) only enforces sigs post-cutoff via _audit_ts_ge_cutoff, but no caller invokes it as a merge gate.
- [confirmed] Bash audit_verify_chain has only 3 real callers, none of which is a merge gate. — Function calls (not defs/comments): audit-snapshot.sh:152 (refuse to snapshot a broken chain), audit-envelope.sh:1130 (internal audit_recover_chain short-circuit), graduated-trust-lib.sh:316 & :1472 (L4 trust-ledger integrity / trust_verify_chain wrapper). post-merge-orchestrator.sh has ZERO audit/verify/model-invoke references (grep -ni audit … → empty). run-mode/run-bridge/auditing-security skills have ZERO verify-chain wiring (grep -rln → empty).
- [confirmed] Trust-store is BOOTSTRAP-PENDING: empty root_signature, keys: [], revocations: []. — grimoires/loa/trust-store.yaml (the default _trust_store_path resolves to grimoires/loa/trust-store.yaml per audit_envelope.py:79): root_signature.signature = "" (line ~empty), keys: [], revocations: []. _trust_store_status() (audit_envelope.py:452) returns BOOTSTRAP-PENDING when !sig and !keys and !revs — exactly this state. signer_pubkey in the file matches the pinned root .claude/data/maintainer-root-pubkey.txt (MCowBQYDK2VwAyEAdXgpsndtu9p+fI3Rw/rqr+LjQYtOrZKsXgHKN974o+M=) byte-for-byte.
- [confirmed] The bootstrap runbook exists at grimoires/loa/runbooks/audit-keys-bootstrap.md. — File present (10617 bytes, 260 lines). Covers Step 1 keygen, Step 2 OPERATORS.md, Step 3 trust-store draft, Step 4 maintainer offline root-sign, Step 5 CI/CD secret patterns, Step 6 rotation. trust-store.yaml header explicitly references it. An older copy also exists under grimoires/loa/archive/2026-05-08-cycle-098-…/.
- [confirmed] MODELINV entries (.run/model-invoke.jsonl) are unsigned. — 1143 entries; sampled head + tail: keys = [event_type, payload, prev_hash, primitive_id, redaction_applied, schema_version, ts_utc] — no 'signature', no 'signing_key_id'. Writer is emit_model_invoke_complete (loa_cheval/audit/modelinv.py:759) calling audit_emit("MODELINV", "model.invoke.complete", payload, log_path) with NO signing_key_id arg. audit_emit only signs when signing_key_id is passed OR LOA_AUDIT_SIGNING_KEY_ID is set (audit_envelope.py:364); that env var is UNSET in this environment and configured nowhere.
- [confirmed] The Python verify_for_merge fail-closed-on-BOOTSTRAP-PENDING contract is already implemented and test-proven (ATK-3). — audit_envelope.py:480-486 — _check_trust_store raises RuntimeError('[TRUST-STORE-BOOTSTRAP-PENDING] … refuses BOOTSTRAP-PENDING (ATK-3)') when status==BOOTSTRAP-PENDING and strict_verify. audit_verify_chain catches it and returns (False, msg) at lines 562-564. Test test_audit_envelope_strict_verify.py:91-95 asserts ok is False and '[TRUST-STORE-BOOTSTRAP-PENDING]'/'ATK-3' in msg. ATK-4 (lines 503-507) requires a readable trust-store under strict; ATK-3 local-pubkey-fallback refusal proven at lines 162-165.
- [partial] trust_cutoff makes the current MODELINV log un-verifiable under ANY strict signature check even after bootstrap — a [STRIP-ATTACK-DETECTED] hazard not mentioned in the original AUDIT-ACTIVATION framing. — trust-store.yaml trust_cutoff.default_strict_after = '2026-05-03T00:00:00Z'. First MODELINV entry ts_utc = 2026-05-10 (post-cutoff), all 1143 entries are June 2026 and unsigned. Under verify_for_merge=True with default LOA_AUDIT_VERIFY_SIGS=1, _ts_ge_cutoff is true for every entry → audit_verify_chain returns BROKEN '[STRIP-ATTACK-DETECTED] signature required post-cutoff' (audit_envelope.py:598-607). So even a fully-bootstrapped trust-store would FAIL the existing log unless (a) signing is enabled going forward AND (b) the pre-signing history is sealed/rotated or the cutoff is advanced. This is a real activation precondition, marked partial because it's an inference about a hypothetical strict run, not a current failure.

### Scope notes
SPRINT SCOPE — 'activate verify_for_merge + bootstrap'. Two halves: (A) a maintainer ceremony that an agent CANNOT fully complete, and (B) an agent-doable wire-in. They must be sequenced A-before-B or B fails closed by design.\n\nMAINTAINER-SECRET BLOCKER (flag — agent cannot do this): Step 4 of grimoires/loa/runbooks/audit-keys-bootstrap.md requires signing the trust-store with the OFFLINE root private key (`~/.cycle-098-root-keys/root.priv`, air-gapped, never network-connected) via `.claude/scripts/lib/audit-signing-helper.py trust-store-sign`. The pinned root pubkey (.claude/data/maintainer-root-pubkey.txt fingerprint e7:6e:ec:46…) is the verification anchor; only the holder of the matching private key (@janitooor, the maintainer) can produce root_signature.signature. NOTE: LOA_AUDIT_KEY_PASSWORD is for decrypting a WRITER's per-agent key (audit_envelope.py:89-107, already DEPRECATED/SKP-002 with --password-fd/--password-file preferred) — it is NOT the root-signing secret. The true blocker is possession of the root private key, which no env var exposes. An agent can prepare the keys[] PR (Steps 1-3) but cannot land Step 4.\n\nCEREMONY STEPS (per runbook, who-does-what):\n1. Agent/operator: generate per-writer Ed25519 keypair to ~/.config/loa/audit-keys/<writer_id>.priv (0600) + .pub (runbook Step 1).\n2. Agent/operator: add self to grimoires/loa/operators.md (Step 2).\n3. Agent/operator: draft keys[] entry in grimoires/loa/trust-store.yaml with pubkey_pem (Step 3); leave root_signature untouched.\n4. MAINTAINER ONLY (cannot delegate to agent): offline-sign JCS canonicalization of {keys, revocations, trust_cutoff} with root.priv, fill root_signature.{signature,signed_at,signer_pubkey}, merge (Step 4). After this _trust_store_status() flips BOOTSTRAP-PENDING→VERIFIED.\n5. Going forward: set LOA_AUDIT_SIGNING_KEY_ID=<writer_id> so cheval.py's MODELINV emit (loa_cheval/audit/modelinv.py:759 → audit_emit) starts signing. Without this, new entries stay unsigned and the cutoff hazard persists.\n\nWIRE-IN POINT (the agent-doable half): The realistic single wire-in is the BASH path, because production uses the bash audit_verify_chain — but the strict fail-closed logic exists ONLY in Python today. Two options:\n  Option A (smallest correct diff): add a strict mode to bash. In audit-envelope.sh, give _audit_check_trust_store a strict flag (mirror Python audit_envelope.py:478-486): when status==BOOTSTRAP-PENDING AND LOA_AUDIT_STRICT_VERIFY=1 (or a --verify-for-merge CLI flag on the verify-chain dispatch at line 1197), block with [TRUST-STORE-BOOTSTRAP-PENDING]/ATK-3 instead of returning 0. Then add ONE caller in post-merge-orchestrator.sh (which today has zero audit refs) that runs `audit-envelope.sh verify-chain .run/model-invoke.jsonl` (and the L1-L7 logs) with strict on, gating the merge/release pipeline. Precedent to copy for the strict branch: Python _check_trust_store (audit_envelope.py:470-490) and _read_trust_cutoff ATK-4 (493-521).\n  Option B (reuse proven Python gate, no new bash logic): have post-merge-orchestrator.sh shell out to `python3 -c 'from loa_cheval.audit_envelope import audit_verify_chain; ...'` with verify_for_merge=True. This reuses the already-test-proven ATK-3/ATK-4 contract (test_audit_envelope_strict_verify.py) verbatim — fewer new lines, edge-case-correct per the simplicity ladder. Recommended over A.\n\nCRITICAL ORDERING / PRECONDITION the framing missed: because trust_cutoff.default_strict_after=2026-05-03 and the entire existing 1143-entry MODELINV log is post-cutoff AND unsigned, turning on strict verify will report [STRIP-ATTACK-DETECTED] on the FIRST existing entry — even with a perfectly bootstrapped trust-store. So the sprint must ALSO decide one of: (i) advance default_strict_after to the bootstrap date so legacy unsigned history is grandfathered and only new (signed) entries are strict-checked; or (ii) seal/rotate the pre-signing log. Sequencing for fail-closed safety: bootstrap (Steps 1-4) → enable signing (Step 5) → set cutoff to grandfather legacy → wire the gate (Option B) → only then is verify_for_merge greenable. Wiring the gate before signing-is-live would hard-fail every merge.

### Suggested beads
- [M] Bootstrap audit trust-store: agent prepares Steps 1-3 (writer keypair gen, operators.md entry, keys[] draft PR for trust-store.yaml); HAND OFF to @janitooor for Step 4 offline root-sign (agent-blocked — maintainer secret).
- [S] Advance/decide trust_cutoff.default_strict_after to grandfather the legacy 1143-entry unsigned MODELINV log (or seal it), so strict verify doesn't [STRIP-ATTACK-DETECTED] on existing history. Must precede gate wiring.
- [S] Enable MODELINV signing: set LOA_AUDIT_SIGNING_KEY_ID for the cheval emit path so new model-invoke.jsonl entries are Ed25519-signed (modelinv.py:759 already passes through audit_emit's env-var signing).
- [M] Wire verify_for_merge gate into post-merge-orchestrator.sh — prefer Option B (shell out to Python audit_verify_chain(verify_for_merge=True), reusing the test-proven ATK-3/ATK-4 contract) over re-implementing strict mode in bash. Gate must run AFTER bootstrap+signing+cutoff are done.
- [S] Add a regression/integration test that the post-merge gate fails closed when trust-store is BOOTSTRAP-PENDING and passes once VERIFIED+signed (mirrors test_audit_envelope_strict_verify.py into the orchestrator path).
- [S] (Optional, if bash gate chosen) Add strict-verify branch to bash _audit_check_trust_store mirroring Python audit_envelope.py:478-486, plus a --verify-for-merge flag on the verify-chain CLI dispatch (audit-envelope.sh:1197).

### Risks
- MAINTAINER SECRET, not an env var: the activation cannot be completed by an autonomous agent. Step 4 (offline root-key sign) needs the air-gapped root private key held by the maintainer. LOA_AUDIT_KEY_PASSWORD is a red herring — it only decrypts a writer key (deprecated SKP-002), not the root. Any sprint must hand off to @janitooor for the signing ceremony.
- Cutoff trap: trust_cutoff.default_strict_after=2026-05-03 < every existing MODELINV ts. Strict verify will [STRIP-ATTACK-DETECTED] on the current unsigned log regardless of bootstrap. Must advance the cutoff (or seal legacy) BEFORE wiring the gate, or the merge pipeline hard-fails on day one.
- Two divergent verifiers: Python audit_verify_chain (has verify_for_merge / ATK-3 / ATK-4 fail-closed) is the one that's test-proven but UNUSED in prod; bash audit_verify_chain (actually used by audit-snapshot/graduated-trust) has NO strict mode. Wiring the gate in bash means re-implementing the strict branch (risk of behavior drift from the Python reference). Option B (shell out to Python) avoids the drift but adds a python3+loa_cheval import dependency to post-merge-orchestrator.sh.
- post-merge-orchestrator.sh currently has zero audit/verify references — adding a gate there is greenfield wiring, not a tweak. Verify it runs in an env where loa_cheval is importable (the repo .loa/venv exists) and where LOA_TRUST_STORE_FILE/LOA_PINNED_ROOT_PUBKEY_PATH resolve.
- MODELINV signing is OFF: emit_model_invoke_complete (modelinv.py:759) never passes signing_key_id and LOA_AUDIT_SIGNING_KEY_ID is unset. Until Step 5 sets it, NEW entries also stay unsigned — so even forward-only strict verify produces nothing verifiable. Enabling signing is part of activation, not a follow-up.
- Zone-system: audit-envelope.sh, modelinv.py, post-merge-orchestrator.sh are all in .claude/ (System Zone) — edits need cycle-level authorization, not ad-hoc. trust-store.yaml and operators.md are in grimoires/ (State Zone, writable).
- BOOTSTRAP-PENDING is per-process cached by (path, mtime, size, sha256) in both bash (_audit_ts_cache_key) and Python (_trust_store_cache_key). After the maintainer signs, a long-running process won't see VERIFIED until the cache invalidates on mtime change — fine for fresh CI runs, a gotcha for tests/daemons.

## Grimoire INDEX + Knowledge-Layer scope (grimoire-index.sh generator + KF/NOTES minimal-conformance writer)

### Claims verified
- [confirmed] No grimoires/loa/INDEX.md exists today — the proposed unified index is greenfield — `ls grimoires/loa/INDEX.md` -> No such file or directory. Top-level grimoires/loa/ has per-family files (known-failures.md, NOTES.md, ledger.json, prd*.md) + subdirs, no INDEX.md. Also no existing grimoire-index.sh: `find . -name grimoire-index*` empty.
- [confirmed] grimoires/loa/a2a/ holds 297 entries (the dir the task asked to count) — `ls grimoires/loa/a2a | wc -l` -> 297. Dominated by ~180 sprint-*/sprint-bug-*/bug-* dirs + cycle-NNN-archive + bridge-iter-N + trajectory/ + audits/ + flatline/.
- [confirmed] Distinct artifact families + ID schemes: KF-NNN (known-failures.md), vision-NNN (visions/entries/vision-NNN.md), lore kebab-ids (lore/patterns.yaml + index.yaml), handoff sha256:<hex> (handoffs/INDEX.md), obs entries (memory/observations.jsonl, NO id field), L-NNNN (memory/learnings.yaml) — KF: heading `## KF-{NNN}: title`, KF-001..KF-020. vision: `**ID**: vision-013` inside visions/entries/vision-013.md..021. lore: index.yaml `id: governance-isomorphism`; patterns.yaml entry `- id/term/short/context/source/tags`. handoff: INDEX row `| sha256:f3308f6... | file | from | to | topic | ts_utc | read_by |`. obs: observations.jsonl objects have timestamp/type/category/title/evidence/detail, NO `id` key. L-NNNN: learnings.yaml `- id: L-0001` (only L-0001 today).
- [confirmed] handoffs/INDEX.md generator is structured-handoff-lib.sh (a lib, not a *-gen.sh) — the canonical INDEX-row writer with sha256 ids, sanitization, flock, idempotency — .claude/scripts/lib/structured-handoff-lib.sh (1485 lines). `_handoff_atomic_publish` (~L595): reads/seeds header `| handoff_id | file | from | to | topic | ts_utc | read_by |`, idempotency by handoff_id (~L630), flock on .INDEX.md.lock, atomic mktemp+rename. handoff_id = sha256 of jcs-canonicalized content subset (L513-543 via lib/jcs.sh).
- [confirmed] Existing *-gen.sh precedents to copy: construct-index-gen.sh (dual YAML/JSON to .run/, validate mode, jq assembly), ground-truth-gen.sh (scaffold/checksum/validate modes), loa-learnings-index.sh (index/query over multiple knowledge sources) — `find .claude/scripts -name '*-gen.sh'` -> trajectory-gen.sh, release-notes-gen.sh, butterfreezone-gen.sh, construct-index-gen.sh, ground-truth-gen.sh, butterfreezone-construct-gen.sh. construct-index-gen.sh (654 L): DEFAULT_OUTPUT=.run/construct-index.yaml, --json -> .run/construct-index.json, sources compat-lib.sh+yq-safe.sh, jq -n assembly, VALIDATE mode + metadata.generated_at. loa-learnings-index.sh (1386 L) already indexes skills/feedback/decisions/learnings.
- [confirmed] sanitize-at-surfacing (L5/L6/L7) is context-isolation-lib.sh::sanitize_for_session_start; INDEX-row injection defense is per-field control-byte rejection, NOT body interpretation — context-isolation-lib.sh:96 `sanitize_for_session_start <L6|L7> <content>`. structured-handoff-lib.sh L26-27 comment: body sanitization happens at SURFACING time in that fn. INDEX rows built only from slug-shape frontmatter fields, each scanned `_re.compile(r'[\x00-\x1f\x7f]')` and refused on control byte (L451-470). Body text never enters the INDEX.
- [confirmed] No KF/NOTES write-helper lib exists; kf-auto-link.py is a READER (parses known-failures.md to drive model-config downgrades), not a writer of KF entries or the Index table — .claude/scripts/lib/kf-auto-link.py (947 L) docstring: parses known-failures.md for (provider,model) KF entries, applies IMP-001 status->recommended_for removal to model-config.yaml + .run/kf-auto-link.jsonl. It never appends KF entries nor maintains the `## Index` table. `grep -rln known-failures .claude/scripts/` -> only compat-lib.sh + kf-auto-link.py. KF/NOTES are hand-edited today.
- [confirmed] KF Evidence-cell schema: `### Attempts` table `| Date | What we tried | Outcome | Evidence |` (Evidence = commit SHA / PR# / run ID); top-of-file `## Index` table is `| ID | Status | Feature | Recurrence |` with anchor links — known-failures.md ## Schema (L14-50): fields Status/Feature/Symptom/First observed/Recurrence count/Current workaround/Upstream issue/Related + Attempts table, Outcome enum DID NOT WORK / WORKAROUND-AT-LIMIT / RESOLVED. KF-018 has live Attempts rows with Evidence cells (`issue #1089 cheval log`, `bd-n56l / issue #1089 fix (2)`). Index header L55 `| ID | Status | Feature | Recurrence |`, rows `| [KF-001](#kf-001-...) | RESOLVED... | feature | 3 |`.
- [confirmed] Artifact bodies are densely cross-referenced (KF-NNN, vision-NNN, #issue, bd-xxxx) — outbound edges extractable by regex without interpreting prose — `grep -oE 'vision-[0-9]+|#[0-9]+|KF-[0-9]+|bd-[a-z0-9]+' known-failures.md | sort | uniq -c` -> KF-002x40, KF-008x12, #844x10, #43333x10, vision-024x9. Fixed regexes per family: KF-\d+, vision-\d+, #\d+, bd-[a-z0-9]+, L-\d{4}, sha256:[0-9a-f]{64}.
- [confirmed] visions/ and handoffs/ already use lock files (.INDEX.md.lock, index.md.lock, .lifecycle.lock) — concurrent-write discipline is an established convention the new generator must honor — handoffs/.INDEX.md.lock (0 bytes), visions/index.md.lock + visions/.lifecycle.lock present. structured-handoff-lib flocks .INDEX.md.lock during atomic publish (L605-617).

### Scope notes
TWO deliverables.

DELIVERABLE 1 — grimoire-index.sh (the unified INDEX generator). New file: .claude/scripts/grimoire-index.sh. Copy the SKELETON from .claude/scripts/construct-index-gen.sh (closest structural match): set -euo pipefail; SCRIPT_DIR/PROJECT_ROOT bootstrap; `source compat-lib.sh` (for sha256_portable per KF-012) + `source yq-safe.sh`; CLI flags `--json` / `--output PATH` / `--quiet` / `--validate`; `if [[ BASH_SOURCE == 0 ]]; then main; fi` guard. Dual emit precedent: YAML to grimoires/loa/INDEX.md (or .run path) + JSON to .run/grimoire-index.json, with a `metadata.generated_at` field (construct-index-gen.sh:79-80 shows the .yaml->.json output-path swap, and the validate mode checks metadata.generated_at).

What it WALKS (one extractor function per family — keep them isolated like construct-index-gen.sh's per-pack loop):
 - KF: grep `^## KF-([0-9]+): (.*)$` in grimoires/loa/known-failures.md -> id, title; pull Status/Feature/Recurrence by matching the bolded field lines under each heading (the `## Index` table already has ID|Status|Feature|Recurrence so you can parse THAT table directly rather than re-deriving — it is the maintained summary).
 - vision: for f in grimoires/loa/visions/entries/vision-*.md: id from filename; `**ID**:` / `**Status**:` / `**Date**:` / `**Tags**:` lines.
 - lore: parse grimoires/loa/lore/index.yaml (categories[].entries[].id/status) + patterns.yaml (`- id:` / term / status) via yq.
 - handoff: parse grimoires/loa/handoffs/INDEX.md rows directly (already the SoT table: handoff_id|file|from|to|topic|ts_utc|read_by) — do NOT re-hash, just surface.
 - obs: memory/observations.jsonl has NO id — synthesize a stable key (e.g. sha256_portable of timestamp+title, or just timestamp+type+category) and surface title/category/timestamp via jq. State this as a design decision in the script header.
 - L-NNNN: memory/learnings.yaml `- id: L-NNNN` / title / type / verified via yq.

Outbound refs per family: regex-extract ONLY id-shaped tokens (KF-\\d+, vision-\\d+, #\\d+, bd-[a-z0-9]+, L-\\d{4}, sha256:[0-9a-f]{64}) from each body to build a cross-reference edge list. NEVER surface free-form body prose into the INDEX.

SANITIZATION (load-bearing, L5/L6/L7 rule): the INDEX must be built from id/path/title/status SLUG fields only. Mirror structured-handoff-lib.sh's defense: reject (or strip) any control byte `[\\x00-\\x1f\\x7f]` from any field that lands in a markdown table cell or JSON string, and never interpret body text as instructions. For titles (which are author-controlled prose), escape markdown table pipes (`|`) and collapse to single-line. Do NOT call sanitize_for_session_start (that is for surfacing L6/L7 bodies into a live session prompt, not for index generation) — but DO adopt its control-byte-rejection posture.

JSON assembly: use `jq -n --argjson ...` exactly as construct-index-gen.sh:259-261. Add a `--validate` mode that checks every family entry has id+path (copy the construct validate loop at construct-index-gen.sh:590-650).

Honor the lock convention: flock grimoires/loa/.INDEX.md.lock during the atomic write (mktemp + rename), matching structured-handoff-lib.sh L605-617.

DELIVERABLE 2 — KF/NOTES minimal-conformance write-helper. There is NO writer lib today (kf-auto-link.py only READS). Build .claude/scripts/lib/kf-write-lib.sh (or kf-append.sh) that: (a) appends a new `## KF-{NNN}` entry with the schema block (Status/Feature/Symptom/First observed/Recurrence count/Current workaround/Upstream issue/Related + `### Attempts` table + `### Reading guide`) per the canonical ## Schema at known-failures.md:14-50; (b) inserts the matching row into the `## Index` table `| [KF-NNN](#anchor) | Status | Feature | Recurrence |`; (c) for recurrence: increments `Recurrence count:` and appends an Attempts row `| Date | tried | Outcome | Evidence |` (Outcome enum DID NOT WORK / WORKAROUND-AT-LIMIT / RESOLVED). Auto-compute next KF-NNN from `grep '^## KF-'`. Generate the anchor slug the same way the existing Index links do (lowercase, non-alnum->-). Append-only discipline: never rewrite existing entries except the three allowed mutations (recurrence_count, attempts rows, status flip) per the file header. Reuse compat-lib.sh sha256_portable if any checksum is needed. Mirror kf-auto-link.py's IMP-005 parse policy (unknown status -> warn+skip, malformed -> exit 2) so the writer and reader agree on the grammar.

PRECEDENT FILE LIST (read before writing): .claude/scripts/construct-index-gen.sh, .claude/scripts/ground-truth-gen.sh, .claude/scripts/loa-learnings-index.sh, .claude/scripts/lib/structured-handoff-lib.sh, .claude/scripts/lib/context-isolation-lib.sh, .claude/scripts/lib/kf-auto-link.py, .claude/scripts/compat-lib.sh, .claude/scripts/yq-safe.sh, grimoires/loa/known-failures.md, grimoires/loa/handoffs/INDEX.md, grimoires/loa/lore/index.yaml, grimoires/loa/memory/learnings.yaml.

### Suggested beads
- S: Write grimoire-index.sh skeleton (CLI flags, dual INDEX.md+.run/grimoire-index.json emit, metadata.generated_at, BASH_SOURCE guard) copied from construct-index-gen.sh
- M: Implement the 6 per-family extractors (KF / vision / lore / handoff / obs / L-NNNN) with regex id-token outbound-ref extraction and control-byte sanitization; obs surrogate-key decision
- S: Add --validate mode to grimoire-index.sh (every entry has id+path; flag KF Index-vs-heading drift) copying construct validate loop
- S: Add flock + atomic mktemp/rename write to grimoire-index.sh honoring the .INDEX.md.lock convention
- M: Build kf-write-lib.sh (append KF entry per ## Schema + insert Index row + recurrence increment + Attempts-row append), append-only/surgical, reusing kf-auto-link.py parse grammar
- S: bats tests for both scripts (index emits valid JSON+MD over a fixture grimoire; KF writer is idempotent and never rewrites existing entries) mirroring tests/unit/kf-auto-link-*.bats
- S: Wire grimoire-index.sh into a workflow boundary / health check or scheduled cycle so the INDEX stays fresh (precedent: construct-index-gen.sh invoked on construct install)

### Risks
- observations.jsonl has NO id field (keyed only by timestamp+title) — the index generator MUST synthesize a stable surrogate key or it cannot reference obs entries; decide and document (sha256_portable of timestamp+title is the consistent choice given KF-012).
- The KF `## Index` table is hand-maintained and can DRIFT from the actual `## KF-` headings (e.g. KF-009 and KF-016/019/020 status cells are verbose multi-sentence blobs). If grimoire-index.sh parses the Index table it inherits drift; if it parses the headings it must re-derive Status/Feature. Safer to parse headings for id/title and treat the Index table as the maintained Status/Recurrence source, then flag mismatches in --validate.
- L5/L6/L7 bodies are UNTRUSTED (CLAUDE.md universal invariant). The generator must NOT interpret body prose and must control-byte-sanitize every field reaching a table cell or JSON string; titles need pipe-escaping for markdown tables. This is the single most important correctness constraint.
- KF/NOTES live in the State Zone (writable) but .claude/scripts/ is System Zone (NEVER edit directly per zone-system.md) — the new generator/writer scripts themselves must be added via the proper cycle/sprint authorization, not ad-hoc Edits. The zone-write-guard hook blocks Write/Edit on .claude/ (per MEMORY: use Bash file-invocation for protected paths during dev).
- sha256sum is non-portable (KF-012, recurrence-documented) — any hashing in the new scripts MUST use sha256_portable from compat-lib.sh, enforced by tools/check-no-raw-sha256sum.sh CI scanner.
- known-failures.md is 157KB and append-only — a full-rewrite writer risks the stash-data-loss class (#555). The KF writer must do surgical append + single Index-row insert, never a whole-file rewrite, and must use atomic mktemp+rename under flock (the handoffs lock pattern), NOT git stash wrapping.
- Concurrent writers: visions/handoffs already ship lock files. The new generator+writer must flock (e.g. grimoires/loa/.INDEX.md.lock) to avoid the read-modify-write lost-update hazard, especially under Agent Teams where only the lead may write state.

## BUTTERFREEZONE → AGENTS.md conversion (rec #3 from okf-icm-comparative-analysis-2026-06-27.md): emit a generated root AGENTS.md, generated-not-hand-edited, with a CI drift gate, also closing eileen issues #1132 + #1135.

### Claims verified
- [confirmed] loa already PARSES AGENTS.md but never EMITS one — confirming rec #3's premise. There is no root AGENTS.md anywhere in the repo and no agents-md generator script. — find . -name AGENTS.md → (empty); ls .claude/scripts/ | grep agents → none; refs only in autonomous-agent/SKILL.md:206,292 + resources/operator-detection.md:48,69-84 + commands/autonomous.md:59
- [confirmed] loa's AGENTS.md 'parsing' is shallow: it is used ONLY as an operator-detection signal, never parsed for governance content. Two surfaces: (a) it's a Moltbot workspace-signature file (any 2+ of AGENTS.md/SOUL.md/IDENTITY.md/HEARTBEAT.md/TOOLS.md/USER.md/WORKLEDGER.md → AI_OPERATOR), and (b) 'File exists AND contains agent configuration → AI_OPERATOR'. — operator-detection.md:46-61 (MOLTBOT_SIGNATURES array, count>=2 → AI_OPERATOR) and :69-84 (§2 AGENTS.md Markers: 'File exists AND contains agent configuration → AI_OPERATOR'). SKILL.md:205-213 lists AGENTS.md under 'Moltbot Workspace Signatures (any 2+ files = AI_OPERATOR)'.
- [confirmed] GOTCHA: emitting a root AGENTS.md will, by itself, push loa's own operator detection toward AI_OPERATOR — AGENTS.md is one of the 2+ Moltbot signature files, and §2 treats its mere presence-with-content as an AI signal. A generated AGENTS.md in the loa repo (which already has no other signature files) is a 1-file signal today, but any repo that also has e.g. SOUL.md (L7 soul-identity) would hit the 2+ threshold. — operator-detection.md:46-61 + :84; SKILL.md:205-213. Mitigation needed: exclude generated AGENTS.md from the Moltbot signature set (require a hand-authored marker) OR document that the generated file is governance-only, not an operator marker.
- [confirmed] butterfreezone-gen.sh is a mature, deterministic, provenance-tagged generator: LC_ALL=C/TZ=UTC determinism, flock/mkdir lock, atomic_write, per-section + total word budgets (3400), AGENT-CONTEXT block, ground-truth-meta with head_sha + per-section sha256 checksums, security redaction, manual-section sentinels. 12 canonical sections. — butterfreezone-gen.sh:17-22,32-101,253-284,2276-2291 (atomic_write), 2190-2239 (generate_ground_truth_meta), 2126-2152 (redact_content), CANONICAL_ORDER :49-62
- [confirmed] The current committed BUTTERFREEZONE.md is STALE — head_sha 2b70d2f / generated_at 2026-05-11 / version v1.147.0, while main is at v1.184.x. No workflow regenerates it; RTFM gate only references the VALIDATOR, not the generator. — BUTTERFREEZONE.md:30 (version: v1.147.0), :229-231 (head_sha 2b70d2f, generated_at 2026-05-11); grep butterfreezone in .github/workflows/ → empty; RTFM references butterfreezone-validate only (CLAUDE.loa.md, verify-invariants.sh)
- [confirmed] The drift-gate precedent (model-config.yaml.checksum lockfile + model-registry-drift.yml) is exactly the pattern to copy: a committed .checksum file + a CI job that recomputes sha256 and fails with ::error:: + a 'how to refresh' message, plus codegen --check jobs that re-run the generator and diff. — .claude/defaults/model-config.yaml.checksum exists; model-registry-drift.yml:56-77 (lockfile-checksum job: recorded vs computed sha256, exit 1 + refresh instructions), :79-114 (--check codegen jobs)
- [confirmed] #1132 is real: README.md says 5 Golden Path commands (/loa,/plan,/build,/review,/ship) at lines 4 and 72-80, but the Quick Start code block at lines 41-45 says '5 commands' and lists only 4 (/plan,/build,/review,/ship — omits /loa). — README.md:4 ('5 Golden Path commands (/loa, /plan, /build, /review, /ship)'), :41 ('# 5 commands. Full development cycle.'), :42-45 (only /plan /build /review /ship). gh issue 1132 acceptance: 'A test/check fails if the canonical Golden Path list and Quick Start drift.'
- [confirmed] #1135 is real AND the count is already wrong: README claims '48 total commands' (lines 5,107,72,181) but .claude/commands/*.md contains 53 files. The current (stale) BUTTERFREEZONE.md reality file independently says 'Slash commands (53 total)'. The 48-vs-53 gap is the deprecated/excluded nuance #1135 calls out. — README.md:5,107 ('48 slash commands'/'48 total commands'); ls .claude/commands/*.md | wc -l → 53; BUTTERFREEZONE.md:46 ('## Slash commands (53 total)'); gh issue 1135 AC: 'Deprecated commands are either counted separately or explicitly excluded.'
- [confirmed] command_type frontmatter CANNOT serve as the category/deprecation source of truth — only 12 of 53 command files set it, inconsistently quoted, with no 'golden' or 'deprecated' value. An explicit excludes/golden list is required to reconcile 53→48. — grep '^command_type:' .claude/commands/*.md → 12 files, values {git,skill,survey,wizard,workflow}; no 'deprecated'/'golden'. Golden 5 all exist as files (loa/plan/build/review/ship.md present).
- [confirmed] The tool-agnostic governance to project into AGENTS.md is machine-parseable and lives in .claude/loa/CLAUDE.loa.md (System Zone, framework-auto-updated) as 31 markdown table rows starting with NEVER/ALWAYS/MUST/SHOULD/MAY. CLAUDE.md (project, hand-edited) has 0 such rows — it imports CLAUDE.loa.md. — grep -cE '^\| (NEVER|ALWAYS|MUST|SHOULD|MAY)' → CLAUDE.loa.md:31, CLAUDE.md:0; CLAUDE.md:1 imports @.claude/loa/CLAUDE.loa.md
- [confirmed] butterfreezone-gen.sh is invoked via the butterfreezone-gen skill which is execute-allowlisted to the two scripts only and write_files:false (it shells out; the script does the write). The SKILL.md zone-constraints already declare 'WRITE: BUTTERFREEZONE.md (output)'. — butterfreezone-gen/SKILL.md:9-16 (execute_commands allowlist), :44-51 (zone_constraints), :32-33 (app permission: none)

### Scope notes
RECOMMENDED DESIGN — emit AGENTS.md ALONGSIDE BUTTERFREEZONE.md (not instead of). They serve different audiences: BUTTERFREEZONE.md = token-efficient codebase MAP (capabilities/architecture/module-map/interfaces, provenance-tagged, for any agent ingesting the repo); AGENTS.md = the 2026 lingua-franca GOVERNANCE+CONVENTIONS file (build/test/PR rules + the NEVER/ALWAYS gates) read first by 25+ tools. Keep BUTTERFREEZONE as-is; AGENTS.md is a thin sibling projection. Do NOT fold one into the other — different schemas, different consumers, different word budgets.

MECHANISM — new generator script `.claude/scripts/agents-md-gen.sh` (mirror butterfreezone-gen.sh's skeleton: LC_ALL=C/TZ=UTC, set -euo pipefail, source compat-lib.sh, flock/mkdir lock via acquire_lock/release_lock, atomic_write, --dry-run/--check/--json flags). DO NOT bolt AGENTS.md emission into butterfreezone-gen.sh — that script is System Zone, heavily tested (6 bats files), and its word-budget/truncation/checksum logic is BUTTERFREEZONE-specific. A sibling script is cleaner and independently testable. Reuse helpers by sourcing or copying: extract_project_description, infer_module_purpose, compute_trust_level_tag patterns.

AGENTS.md CONTENT (plain markdown, NO required frontmatter per the de-facto convention — keep it tool-agnostic; a leading HTML comment '<!-- generated by agents-md-gen.sh — DO NOT EDIT; run /butterfreezone or the generator -->' marks it generated):
  1. Title + 1-line purpose (from extract_project_description).
  2. ## Quick Start — build/test/run commands. Source: package.json scripts / Makefile / Cargo (reuse butterfreezone extract_quick_start logic) PLUS the Golden Path 5 commands for loa repos.
  3. ## Commands — the Golden Path table (/loa /plan /build /review /ship) + a generated total command count line. THIS is where #1132/#1135 are satisfied (see below).
  4. ## Project Governance — project NEVER/ALWAYS/MAY rules parsed from .claude/loa/CLAUDE.loa.md's constraint tables (grep '^| (NEVER|ALWAYS|MUST|SHOULD|MAY)'), rendered as a compact bullet/table list. This is the tool-agnostic projection. Also: the Three-Zone table (System .claude never-edit / State grimoires r-w / App src confirm).
  5. ## Conventions — PR reviewer (@janitooor from CLAUDE.md), branch-from-main rule, commit Co-Authored-By trailer (from CLAUDE.md git section), beads-first note.
  6. ## Context Intake — the 'read known-failures.md first' rule (from CLAUDE.md Context Intake Discipline).
  7. Trailing generated-meta comment: head_sha + generated_at + generator version + a content sha256 (mirror ground-truth-meta but simpler).

#1132 + #1135 SOLUTION (both satisfied by making the AGENTS.md generator the single source for command facts + a drift gate):
  - Create a canonical golden-path list as DATA: `.claude/data/golden-path.json` = {"golden":["loa","plan","build","review","ship"],"excluded_from_count":[...]} . This is the single source for the '5 commands' claim — the generator reads it, and a check can assert README's golden lists match it (closes #1132 'canonical Golden Path list and Quick Start drift').
  - Command count: generator computes total = (count of .claude/commands/*.md) minus an explicit `excluded_from_count` list (deprecated/alias commands — this is HOW you reconcile 53 files → the 48 README claims; #1135 AC requires deprecated counted-separately-or-excluded). Emit the computed count into AGENTS.md AND have the drift gate compare it against README.md's '48 total commands' string.
  - The excludes list resolves the 53-vs-48 gap explicitly (5 are excluded — likely golden-path aliases counted separately, or deprecated like the duplicate gpt-review/toggle-gpt-review). The exact 5 to exclude is an operator decision; surface it in golden-path.json with a comment.

CI DRIFT GATE — new `.github/workflows/agents-md-drift.yml` (copy model-registry-drift.yml structure exactly):
  - paths: AGENTS.md, AGENTS.md.checksum, .claude/scripts/agents-md-gen.sh, .claude/loa/CLAUDE.loa.md, .claude/data/golden-path.json, README.md, the workflow itself, .claude/commands/** (so add/remove/rename a command re-runs the gate).
  - job 1 (lockfile): recorded = tr -d space < AGENTS.md.checksum; computed = sha256sum < AGENTS.md; mismatch → ::error:: [DRIFT-AGENTS-MD] + refresh instructions + exit 1. Commit AGENTS.md.checksum next to AGENTS.md (mirror .claude/defaults/model-config.yaml.checksum).
  - job 2 (regen --check): run agents-md-gen.sh --check (regenerate to a temp file, diff vs committed AGENTS.md, fail on diff) — same shape as gen-adapter-maps.sh --check.
  - job 3 (docs-consistency, satisfies #1132/#1135 AC 'CI fails when command files and docs drift'): assert (a) README golden lists == golden-path.json.golden; (b) README '48 total commands' == computed (files minus excludes). This can live in agents-md-gen.sh --check or a small tools/check-command-counts.sh.

PRECEDENT TO COPY: model-registry-drift.yml:56-114 (lockfile-checksum job + --check codegen jobs) and readme-version-sync.yml (sync-readme-version.sh --check pattern — closest analog for the README-count check; mirror its --check flag convention). Use yq pinned+sha256-verified install block from model-registry-drift.yml if yq is needed.

REGEN WIRING: add agents-md-gen.sh (and butterfreezone-gen.sh, currently un-wired) to a regen step. Cheapest: a post-merge hook or a make/regen target; minimum viable is the CI --check gate that BLOCKS merge until the committed file is fresh (forces local regen). The butterfreezone-gen SKILL.md (Phase 2) should add an AGENTS.md generation step, or a new tiny /agents-md skill aliased into the same skill. Note butterfreezone-gen SKILL.md execute allowlist must add the new script path.

SKILL.md change: butterfreezone-gen/SKILL.md:9-16 execute_commands allowlist — add `.claude/scripts/agents-md-gen.sh`; zone_constraints :44-51 — add 'WRITE: AGENTS.md (output)'. (System Zone edit → requires cycle-level authorization per zone-system.md.)

### Suggested beads
- S: Author .claude/data/golden-path.json — canonical golden-5 list + excluded_from_count list (with per-exclusion justification) reconciling 53 command files → README's 48. Operator decides the 5 exclusions.
- M: Write .claude/scripts/agents-md-gen.sh — sibling of butterfreezone-gen.sh (reuse skeleton: determinism, lock, atomic_write, --dry-run/--check/--json). Emits root AGENTS.md: purpose, Quick Start, Commands (golden table + generated count), Project Governance (NEVER/ALWAYS/MAY from CLAUDE.loa.md + Three-Zone), Conventions (PR reviewer, commit trailer, beads-first), Context Intake (known-failures-first), trailing body-only sha256 meta comment.
- S: Add .github/workflows/agents-md-drift.yml — copy model-registry-drift.yml: lockfile-checksum job (AGENTS.md.checksum, body-only) + agents-md-gen.sh --check regen job. Commit AGENTS.md.checksum.
- S: Add docs-consistency check (tools/check-command-counts.sh or fold into --check) — assert README golden lists == golden-path.json.golden AND README '48 total commands' == (files minus excludes). Closes #1132 + #1135 CI-fails-on-drift ACs.
- S: Fix README.md #1132 — include /loa in the Quick Start block OR reword to '4 execution commands + /loa for orientation' so lines 41-45 match the canonical 5-command claim.
- S: MUST-FIX operator-detection collision — patch autonomous-agent/operator-detection.md + SKILL.md so a GENERATED AGENTS.md (carrying the generated-by sentinel comment) does NOT count as a Moltbot/AI_OPERATOR signal; require a hand-authored marker instead.
- S: Wire butterfreezone-gen SKILL.md — add agents-md-gen.sh to execute allowlist + 'WRITE: AGENTS.md' to zone_constraints (System-Zone, cycle-authorized).
- M: Add bats coverage for agents-md-gen.sh (mirror butterfreezone-gen.bats): determinism (two runs byte-identical body), --check exit codes, count reconciliation, governance-section presence.
- S (optional, fixes asymmetry): give BUTTERFREEZONE.md the same lockfile + --check drift gate and a regen wiring (post-merge or CI), since the committed copy is already 6 weeks stale and nothing regenerates it.

### Risks
- OPERATOR-DETECTION COLLISION (must-fix): emitting AGENTS.md makes loa's own autonomous-agent operator detection lean AI_OPERATOR, because AGENTS.md is a Moltbot signature file (operator-detection.md:46-61) and §2 treats 'AGENTS.md exists + has agent config → AI_OPERATOR' (:84). In loa's repo it's a 1-file signal (sub-threshold for the 2+ Moltbot rule) but flips the §2 marker check; in any consumer repo that also has SOUL.md/IDENTITY.md/etc it hits 2+ and silently forces strict AI gates. FIX before shipping: exclude the GENERATED AGENTS.md from the operator-signal set — e.g. operator-detection.md/SKILL.md should require a hand-authored marker (a non-generated sentinel) rather than mere presence, OR skip files carrying the 'generated by agents-md-gen.sh' comment. This is a real behavioral regression, not cosmetic.
- ZONE BOUNDARY: agents-md-gen.sh, the workflow, the SKILL.md edits, and golden-path.json all live under .claude/ (System Zone — NEVER edit directly per zone-system.md; CLAUDE.md). This entire conversion is an authorized System-Zone change requiring cycle-level approval (a PRD/sprint), not an ad-hoc edit. The generated AGENTS.md itself sits at repo ROOT (App/project zone) — fine to write.
- DETERMINISM is mandatory for any --check drift gate: AGENTS.md must be byte-identical on regen or the gate flaps. Copy butterfreezone-gen.sh's LC_ALL=C/TZ=UTC + sorted finds. The ground-truth-meta block embeds head_sha + generated_at (a timestamp) — those make raw content non-deterministic, so the .checksum/--check must hash ONLY the body (exclude the meta comment), exactly like butterfreezone never checksums its own meta block. Plan the checksum scope carefully.
- THE 53→48 RECONCILIATION IS A JUDGMENT CALL, not derivable from files: command_type frontmatter is set on only 12/53 files and has no 'deprecated'/'golden' value, so there is no mechanical signal for which 5 commands README excludes. The excluded_from_count list must be authored by the operator (likely candidates: golden-path aliases counted as the '5 Golden Path' separately, or near-duplicates gpt-review/toggle-gpt-review). Ship golden-path.json with the explicit list + a comment justifying each exclusion; do NOT guess silently.
- README is the moving target #1135 wants machine-verified, but README.md is project-zone/hand-edited and the count there (48) is ALREADY wrong vs files (53). The drift gate must either (a) update README via a sync script (like sync-readme-version.sh) or (b) only assert consistency and require humans to fix README. Pick (b) for v1 (less blast radius) — the gate fails, human regenerates AGENTS.md + edits README to match the generated count.
- SCOPE CREEP risk: the proposal (#3) is 'M effort, high conf' for AGENTS.md emission + drift gate. #1132/#1135 are separately-filed issues that this design absorbs. Keep them bundled ONLY because golden-path.json + the count-check are the shared mechanism; do not let the AGENTS.md governance-projection (parsing CLAUDE.loa.md tables) balloon — v1 can hardcode/template the governance section and iterate, rather than building a robust markdown-table parser. The CLAUDE.loa.md tables are stable enough to grep simply.
- butterfreezone-gen.sh is currently UN-WIRED to any CI/post-merge regen (only the validator is referenced by RTFM). If you add agents-md-gen with a --check gate but BUTTERFREEZONE has none, you create asymmetry; the committed BUTTERFREEZONE.md is already 6 weeks stale (v1.147 vs v1.184). Consider giving BUTTERFREEZONE the same lockfile+--check treatment in the same cycle, or at minimum note the asymmetry.

## Orchestration/hygiene refactors: (1) file-presence-authoritative workflow position [proposal #8], (2) L4 graduated-trust EXPERIMENTAL relabel [proposal #6], (3) keep-substrate/simplify-wrappers triage [proposal #10]. Driving doc: grimoires/loa/proposals/okf-icm-comparative-analysis-2026-06-27.md sections 6 and 7.3.

### Claims verified
- [confirmed] golden-path.sh ALREADY makes file-presence authoritative for golden-command POSITION; it never reads .run/sprint-plan-state.json. Position is computed from filesystem markers: COMPLETED marker, grep-APPROVED audit gate, engineer-feedback review gate, and PRD/SDD/sprint.md presence. — Read .claude/scripts/golden-path.sh: _gp_sprint_is_complete L78 ([[ -f ${sprint_dir}/COMPLETED ]]); _gp_sprint_is_audited L109 (grep -q APPROVED auditor-sprint-feedback.md); _gp_sprint_is_reviewed L95; golden_detect_plan_phase L50-58 (PRD/SDD/sprint.md). grep -l sprint-plan-state.json over golden-path.sh = no hit.
- [confirmed] The RUNNING-while-committed drift is real and documented: .run state says RUNNING with 0 completed while git+filesystem prove all sprints landed. Current mitigation is a band-aid that rewrites .run state from a git cross-reference. — .claude/skills/run-mode/SKILL.md:655-695 'stuck at state RUNNING with 0 completed sprints even though git history shows all sprint commits already landed'; simstim-orchestrator.sh --sync-run-mode cross-references git as SECONDARY source then rewrites state=JACKED_OUT with git_inferred:true. Live .run/sprint-plan-state.json is JACKED_OUT 3/3 (consistent now), but the recovery path proves drift is a known recurring mode.
- [confirmed] .run/sprint-plan-state.json mixes POSITION fields (state, sprints.current, sprints.completed, sprints.list[].status) with AUTONOMY/circuit-breaker counters; the counters already live in separate files, so demoting position to a derived cache is clean. — .run/sprint-plan-state.json shape: {state, sprints:{total,completed,current,list[{status}]}, options:{max_cycles,timeout_hours}, metrics:{total_cycles}}. Counters are external: run-mode/SKILL.md:278 'circuit-breaker.json | Trigger counts, history'; ~17 .run/circuit-breaker-*.json files present; cycle_count at SKILL.md:513.
- [partial] next-bug-sprint-id poison is only PARTIALLY fixed. The malformed dir-name parse is rejected (exact anchor), but the in-file grep still harvests a stray sprint-bug-N token from a markdown body, inflating the next id. True next should be 227 (counter 226); script returns 623. — PROJECT_ROOT=$PWD bash -x next-bug-sprint-id.sh: disk_max=622 (from grimoires/loa/a2a/bug-20260615-i1064-2e8933/sprint.md which references token sprint-bug-622), origin_counter/claims=226, final next=623. ledger.json global_sprint_counter=226. Lines 76-84 (in-file grep) conflate 'N referenced in a body' with 'highest claimed id' — the exact position-authority confusion #8 targets. Lines 90-100 correctly reject the malformed sprint-bug-622-623 DIR.
- [confirmed] L4 graduated-trust has ZERO runtime footprint: disabled in config example, absent from active config, no trust ledger on disk, and no runtime caller of trust_grant/trust_query outside its own lib/skill/docs/schemas. — .loa.config.yaml.example L4 enabled:false; grep graduated_trust .loa.config.yaml = absent; find . -name trust-events.jsonl / *trust*ledger* = empty; find .run -iname *trust* = empty; trust_grant callers = graduated-trust-lib.sh + skills/graduated-trust/SKILL.md + CLAUDE.loa.md + agent-network-reference.md + 2 schema files only (defs/docs, no invocation). graduated-trust-lib.sh:13-26 header: '4B TODO / 4C TODO / 4D TODO' (only slice 4A shipped).
- [confirmed] The L4 relabel touches System Zone files and so requires a cycle-level /implement, not an ad-hoc edit. Targets are the lib header and the agent-network-reference L4 section. — Targets: .claude/scripts/lib/graduated-trust-lib.sh:1-30 and .claude/loa/reference/agent-network-reference.md:55-74. zone-system.md: '.claude/ is the System Zone — agents MUST NOT edit directly; Authorized System Zone writes require explicit cycle-level approval in the PRD.' CLAUDE.md NEVER rule: app code outside /implement bypasses gates.
- [confirmed] The four overlapping orchestrators are run-mode, run-bridge, simstim-workflow, spiraling (autonomous-agent is a fifth heavy body). These are prose-heavy SKILL.md WRAPPERS = candidate-for-later simplification; the underlying scripts + cheval multi-model layer are the substrate = KEEP. — SKILL.md line counts: simstim-workflow 1048, autonomous-agent 1180, run-mode 695, run-bridge 326, spiraling 208. Substrate (KEEP): spiral-harness.sh 69KB, bridge-orchestrator.sh 45KB, simstim-orchestrator.sh, spiral-orchestrator.sh, flatline-orchestrator.sh, run-mode-ice.sh, lib/harness-fs-guard.sh + cheval. Proposal sec6 #10 / sec7.3: 'multi-model/audit/autonomy substrate is essential (keep); accidental complexity is the prose-heavy skill bodies, 4 overlapping orchestrators; bound simplify to the wrappers, never the substrate.'
- [confirmed] A drift-repro test must exist per proposal #8 ('Ship with a drift-repro test'). None exists today — workflow_drift_gate.bats is a different drift (workload-tier-map), and test_golden_path.bats has no position-vs-.run-state assertion. — tests/integration/test_golden_path.bats:1-40 hermetic setup (mktemp, copies golden-path.sh, LOA_USE_LEGACY_PATHS=1). tests/integration/workflow_drift_gate.bats header: 'workload_tier_map drift-gate logic tests' (unrelated). No test asserts filesystem-position-wins-over-.run-state.

### Scope notes
AREA 1 — file-presence authoritative for workflow position (proposal #8, L effort). KEY INSIGHT: golden-path.sh is ALREADY file-presence-authoritative (it reads COMPLETED/APPROVED markers + PRD/SDD/sprint.md, never .run state). The drift lives in the .run-state-trusting paths: run-mode/SKILL.md resume logic (state==RUNNING => 'resume, do NOT ask') and next-bug-sprint-id.sh. Concrete scope: (a) Write a contract doc into grimoires/loa or a runbook declaring the precedence: filesystem markers (COMPLETED, auditor APPROVED, PRD/SDD/sprint.md presence) are AUTHORITATIVE for POSITION; .run/sprint-plan-state.json position fields (state, sprints.current, sprints.completed, sprints.list[].status) are a DERIVED CACHE that may be stale and MUST be reconciled against the filesystem before use; circuit-breaker/autonomy counters (.run/circuit-breaker-*.json trigger counts, max_cycles, cycle_count, timeout) remain first-class .run state (they have no filesystem analog). (b) Provide a reconciliation helper (model it on simstim-orchestrator.sh --sync-run-mode which already does the git+filesystem cross-reference and rewrites state with git_inferred:true) and make the run-mode/simstim recovery path call it BEFORE trusting state==RUNNING. Precedent to copy: golden-path.sh _gp_sprint_is_complete/_gp_sprint_is_audited (the existing authoritative readers) and the existing --sync-run-mode reconcile. (c) Fix next-bug-sprint-id.sh: drop or de-weight the in-file grep source (lines 76-84) that harvests sprint-bug-N TOKENS from markdown bodies — it produced disk_max=622 from a reference inside bug-20260615-i1064-2e8933/sprint.md, giving next=623 when the true next is 227. The ledger global_sprint_counter (226) + cycle CLAIMS are the authoritative position sources; the in-file body grep is a content-scan masquerading as position and is the live poison. NOTE all run-mode/next-bug-sprint-id edits are System Zone (.claude/) => cycle-level /implement.\n\nAREA 2 — L4 EXPERIMENTAL relabel (proposal #6b, L effort, quick win). Edit two files, DELETE NOTHING: (1) .claude/scripts/lib/graduated-trust-lib.sh header (lines 1-30) — add an EXPERIMENTAL / BUILD-AHEAD / NOT-LOADED-BY-DEFAULT banner noting only slice 4A shipped (4B/4C/4D are TODO stubs) and zero runtime footprint; (2) .claude/loa/reference/agent-network-reference.md L4 section (lines 55-74) — relabel L4 as EXPERIMENTAL/dormant, note enabled:false default + standards-contribution intent, stop presenting it as a live guarantee. Both are System Zone => requires a /implement cycle with PRD authorization (cannot ad-hoc edit per zone-system.md). No code/behavior change; documentation/header only.\n\nAREA 3 — keep-vs-candidate triage (proposal #10, S effort, KEEP decision, no deletion to scope). KEEP (substrate, do not touch): cheval multi-model layer, spiral-harness.sh, bridge-orchestrator.sh, simstim-orchestrator.sh, spiral-orchestrator.sh, flatline-orchestrator.sh, run-mode-ice.sh, lib/harness-fs-guard.sh, and the independent review/audit gates. CANDIDATE-FOR-LATER (prose-heavy wrappers, simplification only when explicitly scoped, NOT now): the four overlapping orchestrator SKILL.md bodies — simstim-workflow (1048), run-mode (695), run-bridge (326), spiraling (208), plus autonomous-agent (1180). This area is a CONFIRMATION/boundary-setting deliverable, not an implementation task: the explicit guardrail (from proposal sec7.3) is 'bound any simplify pass to the wrappers, never the substrate.'

### Suggested beads
- [S] Relabel L4 graduated-trust EXPERIMENTAL/dormant in graduated-trust-lib.sh header + agent-network-reference.md L4 section (lines 55-74); delete nothing; note enabled:false default + only-slice-4A-shipped. System Zone => /implement cycle. (proposal #6b)
- [S] Write the file-presence-authority contract: filesystem markers (COMPLETED/APPROVED/PRD/SDD/sprint.md) AUTHORITATIVE for position; .run position fields are a derived cache; circuit-breaker/autonomy counters stay first-class. Doc into runbooks or zone-state.md. (precursor to #8)
- [L] MANDATORY drift-repro bats test: hermetic temp repo, write .run/sprint-plan-state.json with state=RUNNING/sprints.completed=0 while creating filesystem COMPLETED+APPROVED markers for all sprints; assert the position resolver reports complete (filesystem wins) and the reconcile helper rewrites the stale .run cache; assert circuit-breaker counters are preserved untouched. Model on tests/integration/test_golden_path.bats setup. (proposal #8 'ship with a drift-repro test')
- [M] Fix next-bug-sprint-id.sh: de-weight/remove the in-file body grep (lines 76-84) that harvested sprint-bug-622 from a markdown body (gave next=623, true next 227); make ledger global_sprint_counter + cycle claims + correctly-anchored dir scan the only position sources. System Zone => /implement cycle. Add a regression test pinning next-id to ledger+1 given a stray body token.
- [M] Add reconcile-before-trust to run-mode/simstim resume path: call the filesystem-vs-state reconciliation (extend simstim-orchestrator.sh --sync-run-mode to use COMPLETED/APPROVED markers, not just commit-counting) BEFORE acting on state==RUNNING 'resume, do NOT ask'. System Zone => /implement cycle.
- [S] Record the KEEP-vs-candidate boundary (Area 3): KEEP cheval substrate + 7 orchestrator scripts + review/audit gates; mark the 4 prose-heavy orchestrator SKILL bodies (+autonomous-agent) as later-simplification-only with the explicit 'never the substrate' guardrail. Confirmation note, not an implementation. (proposal #10)

### Risks
- L4/run-mode/next-bug-sprint-id are ALL System Zone (.claude/) — zone-system.md forbids direct edits; every change here needs a cycle-level /implement with PRD authorization. The zone-write-guard hook blocks Write/Edit on .claude/ but NOT Bash, so an ad-hoc Bash patcher would silently bypass the gate (do not do this).
- next-bug-sprint-id 'poison' is subtler than memory's note ('still poisoned 623'): the DIRECTORY parse (lines 90-100) is correctly fixed; the live defect is the IN-FILE grep (lines 76-84) harvesting a legitimate sprint-bug-622 reference from a markdown body. Removing source #2 entirely risks losing the disk-based safety net for ids claimed-on-disk-but-not-in-ledger; prefer de-weighting to body-grep-only-when-ledger-absent, or restricting the grep to the directory-name layout that source #2b already handles correctly.
- The RUNNING-drift band-aid (--sync-run-mode git inference) has a documented known limitation (counts commits by prefix; multiple matching commits per sprint can cause early completion). A file-presence-authoritative rewrite should prefer the COMPLETED/APPROVED markers (already used by golden-path.sh) over commit-counting as the position source.
- Demoting .run position to a derived cache must NOT touch the circuit-breaker/autonomy counters (max_cycles, cycle_count, timeout, trigger history) — those are genuinely first-class .run state with no filesystem analog; conflating them would break circuit-breaker protection.
- Any model-config-adjacent or schema-adjacent change triggers CI drift gates (per memory: regenerate generated-model-maps + BB dist + checksum). The L4 relabel and next-bug-sprint-id fix do not touch model-config, but if a schema (.claude/data/trajectory-schemas/trust-events/) is touched during the L4 relabel, expect schema-guard CI to fire.
- Do NOT let an over-zealous simplicity pass delete the multi-model substrate (proposal sec7.3 explicit guardrail). Area 3 is a KEEP decision; scoping deletion of any orchestrator SKILL body is out-of-scope and dangerous.

## Foldable issues
Verdict: the version/command-count/docs-claim cluster (#1132, #1135, #1143, #1149, #1130, #1124) FOLDS — all six are the same primitive: machine-verify a hand-maintained README/AGENTS.md claim against its canonical source and fail CI on drift. They collapse into the cycle's generated-truth-surface work (BUTTERFREEZONE→AGENTS.md export + ingest-contract doc), with #1143 as the umbrella acceptance criterion and the rest as concrete fields (golden-path list, command count, agent count, version surface). Implement once as a generated-consistency check; do NOT spin separate machinery per issue.\n\nThe copied-artifact drift pair (#1138, #1150) does NOT fold — despite the task's tentative grouping, both target .claude/scripts/lib/symlink-manifest.sh copied-vs-referenced artifacts, source-commit metadata, and doctor/update-reconcile freshness. That is the installer/update-path program (with #1123/#1131/#1136/#1144/#1151/#1152/#1153), which touches none of this cycle's files. Keep it as its own cycle so the plan doesn't absorb the installer-audit program.\n\nzkSoju PRs: all three are SEPARATE and do NOT conflict with this cycle's files. #1108 (construct-index admits construct.yaml-only v4 packs — construct-index-gen/verify.sh), #1109 (AgyHeadlessAdapter repoint gemini-headless→agy — cheval providers + model-config.yaml), #1110 (argv-prompt headless adapters walk instead of crash on un-execable argv — gemini/claude headless adapters). All touch cheval/construct infrastructure, zero overlap with OKF/ICM adoption files (grimoire index, AGENTS.md export, inputs manifest, verify_for_merge, L4, KF/NOTES helper, recurrence detector, fail-soft/ingest-contract docs). They are independent in-flight work — review/merge on their own track. One adjacency note: #1124's installer-manifest-version field and #1153's 'framework truth surfaces' touch the boundary; fold only the doc/agent-context parity, and let the installer-version + audit-lane concerns stay in the separate program.

### FOLD into this cycle
- #1132 — Quick Start says five Golden Path commands but shows four → BUTTERFREEZONE→AGENTS.md export + ingest-contract doc (the generated-truth-surface rec); add the golden-path list as one more machine-checked claim
- #1135 — command-count (48) claims need generated verification → Generated-consistency check for agent-facing claims (BUTTERFREEZONE/AGENTS.md export + ingest-contract); command count becomes one generated field
- #1143 — public documentation claims need generated consistency checks → BUTTERFREEZONE→AGENTS.md / ingest-contract doc — adopt this as the umbrella acceptance criterion (README numeric/interface claims machine-checked)
- #1149 — require docs-generation proof for README command and agent claims → Generated-consistency check rec (BUTTERFREEZONE/AGENTS.md export); same check, auditor-evidence wording
- #1130 — add version-surface parity tests → Generated-consistency check rec; version surface is one more parity field alongside command/agent counts
- #1124 — require version-surface diff check in release PRs → Generated-consistency check rec (version-surface parity). Note: the installer-manifest-version field of this issue belongs to the separate installer-audit cycle; fold only the doc/agent-context/changelog parity here

### SEPARATE (do NOT absorb — installer/release program)
- #1138 — copied framework artifacts need drift detection after updates
- #1150 — require copied-artifact parity evidence after submodule update changes
- #1123 — manifest coverage proof when .claude runtime files are added
- #1131 — construct-manifest adversarial fixtures
- #1136 — submodule integrity mode should be explicit about protections
- #1144 — state relocation is a high-consequence path
- #1151 — require install-mode matrix evidence before declaring releases stable
- #1152 / #1153 — [codex] audit-lane PRs (installer trust root/filesystem safety; framework truth surfaces and release evidence)