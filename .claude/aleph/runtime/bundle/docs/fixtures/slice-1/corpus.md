# Slice 1 — Bounded Synthetic Corpus

> **Synthetic fixture corpus.** Every fragment below is invented for this
> fixture. It is *modeled after* Freeside / token-gating / business-research
> material but contains **no** real ChatGPT export content, **no** real
> competitor claims, and **no** market conclusions that require citation
> verification. This corpus is **instance #1, not Aleph's scope or boundary.**

This corpus is the bounded input the Slice 1 Research Précis (`precis.md`) is
distilled from. It contains exactly three source fragments, with stable source
IDs `SRC-001`, `SRC-002`, and `SRC-003`.

---

## SRC-001 — synthetic deep-research snippet ("access model")

> *Format: a short mock deep-research note. Synthetic.*

Freeside's core access-control primitive is **token-gated membership**: holding
at least a threshold amount of the project token unlocks the gated community
channels. This is the load-bearing mechanic everything else hangs off.

Observation: when access is gated, members appear to stay engaged longer —
**gated access keeps members engaged** over time.

Open architecture point: it is not yet decided whether the gating token should
be a single fungible token or a multi-token (per-tier) arrangement. This choice
is unresolved at the research stage and depends on a later architecture call.

---

## SRC-002 — synthetic conversation excerpt ("tiers & context")

> *Format: a mock chat excerpt between two teammates. Synthetic.*

**A:** People keep asking for *levels* — like a plain holder vs a contributor vs
a core member. We should support **tiered holder levels** so membership isn't
all-or-nothing.

**B:** Agreed, tiers are wanted. Also worth remembering this isn't exotic —
**token-gated communities are pretty common across the market right now**, so
we're not inventing the category, just executing it well.

**A:** Right. And the gated perks are what actually pull new people in —
**gated perks drive referrals**, that's the growth loop we care about.

---

## SRC-003 — synthetic design note ("portal & growth")

> *Format: a mock design/working note. Synthetic.*

Design jots for the gated portal:

- Retention: **token gating improves retention** — keep the gate, it's working.
- Big idea (flagged ambitious): maybe **Freeside should launch its own L2 chain**
  to host all of this end-to-end.
- Cosmetic: the gated portal's **accent color should probably be teal**.
- Referrals: still **undecided whether referral rewards should be paid in the
  token or in non-token points** — needs a decision before build.
