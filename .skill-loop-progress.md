# Skill Loop Progress
# Skill: extreme-software-optimization (/home/merlin/.claude/skills/extreme-software-optimization/SKILL.md)
# Target: loa repo hot paths — per-tool-call hooks (.claude/hooks/safety/, audit/, quality/, .claude/scripts/cleanup-context.sh) + workflow-boundary scripts (beads-health.sh, check-updates.sh, golden-path.sh)
# Total Passes: 10
# Branch: feat/skill-loop-extreme-optimization (from feature/cycle-115-okf-followup-mempalace @ 4ae8aa5f)
# Started: 2026-07-05

## Status: IN PROGRESS — Pass 7 of 10

## Why this target
Every single tool call in a Loa session spawns ~11 hook processes (8 PreToolUse
+ 3 PostToolUse); Stop/SessionStart/UserPromptSubmit add more. These are the
p95-latency-defining hot paths of the framework itself. `beads-health.sh` runs
at every workflow boundary. hyperfine is NOT installed — passes use the shared
timing harness committed by pass 1. Tests: `npx bats tests/unit/<file>.bats`,
full suite `tests/run-unit-tests.sh`.

## Safety floor (applies to EVERY pass)
- Safety hooks are security fences: behavior must be provably identical
  (golden outputs + bats). Never weaken a block pattern or change
  fail-open/fail-closed semantics.
- One mission per pass; Score ≥ 2.0 changes only; isomorphism proof per change.
- No git stash (see .claude/rules/stash-safety.md). LC_ALL=C where sort order matters.

## Missions
1. Baseline & profile: timing harness + per-hook/per-script baselines + golden outputs + opportunity matrix. NO optimization changes.
2. Fork/exec reduction in per-tool-call safety hooks: bash builtins replace external grep/sed/awk/cut/subshell pipelines.
3. jq single-pass consolidation: hooks calling jq repeatedly on the same payload collapse to one invocation (or provably-sound pure-bash extraction).
4. Redundant I/O & lazy sourcing: audit/quality hooks + cleanup-context.sh — batch reads, defer init, fast-path exits.
5. Boundary-script startup latency: beads-health.sh, check-updates.sh, golden-path.sh — short-circuit fast paths, cache config parses.
6. Memoization & caching of pure lookups: repo-root/git/config resolution repeated across hooks per tool call; safe caching with invalidation.
7. Algorithmic wins on growing data: .run/audit.jsonl, observations.jsonl scans — full-scan → tail/indexed, O(n²) → O(n).
8. Pattern-matching efficiency in block-destructive-bash.sh: reorder/combine for early exit WITHOUT weakening coverage; golden parity required.
9. Round-2 re-profile: fresh profile after passes 2–8 (bottlenecks shift), new opportunity matrix, implement Score ≥ 2.0 items.
10. Final verification & proof: full bats suite, golden checksum verify, before/after benchmark table, isomorphism proof summary.

## Zone-guard protocol (passes that edit .claude/hooks/**)
zone-write-guard BLOCKS Write/Edit tool calls to framework zone in this
session (verified empirically; runbook override LOA_ZONE_GUARD_BYPASS=1 is
env-based and cannot be applied to tool calls mid-session). This repo IS
upstream — hook edits are the product. Protocol, per hook change:
1. Author modified hook via Write tool → grimoires/loa/perf/skill-loop-2026-07-05/staging/
2. bash -n; then overwrite the hook's copy INSIDE the bench mirror and prove
   golden parity there FIRST (live session hooks untouched until proven)
3. Atomic install via Bash cp+mv (mutation-logger audits it); then
   golden/capture.sh --verify (all 32 combos) + relevant bats
4. If anything fails: git checkout -- the hook immediately
Deviation is deliberate, operator-invoked, logged here + in final report.

## Completed Passes

### Pass 1 — Baseline & Profile — 2026-07-05
- Files changed: 54 new files under grimoires/loa/perf/skill-loop-2026-07-05/ (bench.sh, bench-env.sh mirror builder, run-matrix.sh, exec-census.sh, 11 payloads, 32 goldens + capture.sh --verify gate, baseline.md, results.tsv, census.tsv)
- Headline: per-tool-call hook overhead Bash ≈57ms / Write ≈132ms / Edit ≈142ms / Stop ≈39ms mean. Cost model: fork+exec ≈1.4ms, jq spawn ≈3ms, yq ≈4-7ms. Top hotspots: karpathy-diff 62-66ms (7 jq + 2 yq), block-destructive 40-47ms (15-20 grep), N-jq class (stop-guard jq×7, mutation-loggers, spiral-guard), zone-write-guard 27-35ms (yq×2). Projected post-loop: Bash ≈18ms, Write/Edit ≈42ms.
- Already fast (skip): team-role-guard benign paths (1-2ms), post-compact no-marker, settings-cleanup <64KB, cleanup-context early exit, team-skill-guard.
- Commit: 1da1afa8
- Verdict: PRODUCTIVE

### Pass 2 — Fork/exec reduction in per-tool-call safety hooks — 2026-07-05
- Files changed (all 9 of .claude/hooks/safety/, installed via the staged zone-guard protocol above; staging copies retained in grimoires/loa/perf/skill-loop-2026-07-05/staging/): block-destructive-bash.sh, team-role-guard.sh, team-role-guard-write.sh, team-skill-guard.sh, spiral-dispatch-guard.sh, spiral-skill-sentinel.sh, adversarial-review-gate.sh, zone-write-guard.sh, run-mode-stop-guard.sh
- Levers: `echo|grep -qE` dispatches → `[[ =~ ]]` over an explicit grep line-model (mapfile split; per-line matching preserved byte-for-byte); `\b` → portable explicit boundary classes (BSD libc regcomp has no \b; the `--delete\b.*--force` shape needed the `([^[:alnum:]_].*)?` form — naive consuming translation diverges on `--delete--force`); `grep -qiE` → `${line,,}` + lowercased pattern under new `export LC_ALL=C` pins; `$(cat)` stdin → direct-jq-on-stdin or `IFS= read -rd ''`; `date -u` → `TZ=UTC0 printf -v '%(…)T'`; `dirname` → parameter expansion with POSIX edge cases; heredoc-`cat`s → printf; `$(pwd)` → $PWD.
- Spawn counts (census): bdb benign 17→2, mutating 17→2, destructive 22→3, blocked 12→9; team-role-guard TEAM 13→1; adv-gate COMPLETED 11→3, early 2→1; zone-guard 7→4 (ALLOW) / 7→3 (BLOCK); spiral-guard 2→1; spiral-sentinel 2→1; stop-guard 8→7 (jq×7 = pass-3 scope).
- Same-window A/B (interleaved, mean ms): bdb benign 49.5→14.1, mutating 46.8→15.8, destructive 61.3→20.3, blocked 40.5→34.7; trg TEAM 35.8→8.5; trgw TEAM 19.5→14.9; zwg 34.9→30.5 / 28.7→23.1 / 42.8→33.4; adv-gate completed 34.9→26.0 (early-exit ~wash, jq-bound); tsg TEAM 11.1→9.1; sss 11.3→8.6; spiral-guard 9.2→6.5; stop idle 42.5→40.7.
- Proofs: 320-case old-vs-new differential corpora (byte-identical exit+stdout+stderr, + trajectory JSONL and sentinel files where written) = 0 divergences; mirror-golden with staged overlay 32/32 BEFORE install; post-install golden 32/32; bats 175 ok / 0 fail (block-destructive-bash ×125, limitations ×6, rm-home-precision ×6, agent-teams-hooks ×8, zone-write-guard ×14, stop-guard-bg-tasks ×7, hook-wiring ×9).
- Deliberate skips (isomorphism not provable): P10 DELETE-FROM `grep -oiE` multi-match extraction (needs case-preserving match offsets), rm_segments extraction, FR-SZ inner write-intent greps (`X\b[^|;&]*` consuming-translation trap; rare path), all `grep -oE|head -1` matched-substring extractions (block paths, exit-2-adjacent), realpath everywhere (symlink semantics load-bearing), git rev-parse, jq/yq counts (pass 3).
- Artifacts: results-pass2.tsv (full matrix; NOTE host was globally 20-70% slower this window — floors confirm — use ab-pass2.tsv for honest before/after), census-pass2.tsv, ab-pass2.tsv.
- Verdict: PRODUCTIVE

### Pass 3 — jq/yq single-pass consolidation — 2026-07-05
- Files changed (7 hooks, installed via the staged zone-guard protocol; staging retained in staging/pass3/): quality/karpathy-surgical-diff-check.sh, safety/run-mode-stop-guard.sh, safety/zone-write-guard.sh, safety/adversarial-review-gate.sh, safety/spiral-dispatch-guard.sh, audit/mutation-logger.sh, audit/write-mutation-logger.sh
- Levers: N-jq → ONE jq per hook with NUL-delimited multi-field extraction (`[0]|implode` builds the NUL; `denul` string-division strips decoded backslash-u-0000 + `sub("\n+$";"")` strips trailing newlines — together replicating bash `$()` capture byte semantics); audit/state/trajectory JSONL lines pre-built inside the single jq (`tojson` == old `jq -nc --arg` encoder, key order preserved) and printf-appended; `now|todate` + `gmtime|strftime` replace date spawns; karpathy 2-yq+7-jq+wc/tr/cat → 1 yq (two guaranteed-single-line decision tokens) + 1 jq (line counts via `slines`/recursive `jl` — host-verified ≡ `jq -r|wc -l` incl. pretty container rendering; files_mod/tool_calls computed analytically pre-append, ≡ old post-append reads incl. unterminated-final-line); zone-guard per-zone yq loop → 1 yq emitting `zone TAB pattern-line` rows in the fixed framework→project→shared order (first match wins; tab split on FIRST tab keeps tab-bearing patterns intact); adv-gate 2 yq → 1 yq tokens, with the 2 raw reads RE-RUN only on the rare BLOCK path for message-byte parity; spiral-guard sentinel-`-f` test hoisted above the stdin jq (0 spawns on the ~99.9% inactive path — every sentinel-absent input exits 0 in the old code too).
- Staged-file review (this pass verified another agent's staging): 4 defect classes found and FIXED before install — (1) missing `$()` trailing-newline strip on extracted fields (decision-relevant: wml file_path=`"\n"` must stay no-append; stop-guard state=`"RUNNING\n"` must still soft-block); (2) mutation-logger lost the old `jq >> file || true` file-CREATION side effect when the line builder fails (empty `: >>` touch restored); (3) non-string (container) field values made jq `join` error → wrong skip/exit-5 where old case-fell-through exit 0 (type guard `if type=="string" then . else tojson end`); (4) karpathy trajectory `threshold` with leading zeros — first fix hypothesis (skip write, assuming --argjson rejects "007") was itself WRONG: differential corpus proved jq --argjson PARSES 007→7 and old wrote `"threshold":7`; replicated with textual zero-strip. Also verified-NOT-defects on this host: yq 4.44.1 `tostring` ≡ default rendering for every probed shape (quoted/single-quoted scalars render BARE, containers/anchors/block scalars identical).
- Spawn counts (census, pass2→pass3): karpathy warn 22→7 (jq 7→1, yq 2→1, date×3/wc×2/tr×2/cat gone); stop-guard idle 7→1; wml 6→2; ml benign 4→2, mutating 9→5; zwg ALLOW 4→3 (yq 2→1), BLOCK 3→3; spiral-guard 1→0; adv-gate early 1→1, COMPLETED-block 3→4 (deliberate: message-parity re-read; the steady valid-artefact COMPLETED path is 2→1 yq).
- Same-window A/B (ab-pass3.tsv, 20-run blocks, mean ms): kp write-warn 66.6→26.6 (p95 87.6→35.9), kp edit-warn 48.1→28.3; stop idle 21.1→5.0; ml benign 15.5→12.5, mutating 22.1→14.8; wml grim 13.6→6.5, edit 16.4→8.0; zwg grim 27.0→23.0, edit-tests 35.1→17.9; spiral 7.5→3.1. Governor drift produced 3 suspect pairs → re-measured with per-run ALTERNATION (ab-pass3-alternating.tsv): advgate grim 7.45→7.36 (wash — path byte-identical), zwg claude-BLOCK 19.4→21.4 (+2ms real: single yq emits all 3 zones where old stopped at framework; ALLOW paths dominate sessions), advgate completed-BLOCK 25.3→33.5 (+8ms real, fires ≤once/sprint on missing artefacts — accepted for message parity); alternation also confirms zwg grim 25.9→20.4, ml benign 18.0→12.4, stop idle 32.3→9.1.
- Proofs: 119-case strict differential corpus (old vs staged; stdout+stderr+exit + full side-effect tree, ts-normalized) = 0 divergences; 4 XFAIL cases inspected and each matches its documented out-of-contract divergence (multi-doc mid-stream-fail prefix logging; container values compact-vs-pretty rendering); jq/yq semantics probes (fromjson whitespace, join/tostring, jl/slines, 26-config yq corpus incl. broken YAML + live config, zone row-stream ≡ on live + adversarial zones.yaml); mirror-golden with staged overlay 32/32 BEFORE install; post-install golden 32/32; bats 48 ok / 0 fail (karpathy-surgical-diff-hook ×6, karpathy-trajectory-event ×4, cycle-114-stop-guard-bg-tasks ×7, zone-write-guard ×14, agent-teams-hooks ×8, hook-wiring ×9).
- Deliberate skips / accepted divergences (all Claude-Code-unproducible inputs): multi-doc stdin erroring mid-stream (old logged truncated prefix, new skips); container-valued command/file_path/tool_name (compact tojson vs old pretty render); write-only(mode-200) state files (counts 1/1 vs old 0/0); bash "ignored null byte" stderr warning gone; iteration/state containers render compact in stop-guard reasons. block-destructive-bash jq and settings-cleanup jq×6 left for their dedicated passes (8, 4/10).
- Artifacts: census-pass3.tsv, ab-pass3.tsv, ab-pass3-alternating.tsv, staging/pass3/ (installed copies).
- Verdict: PRODUCTIVE

### Pass 4 — Redundant I/O elimination & lazy init (Stop/UserPromptSubmit + audit/quality stragglers) — 2026-07-05
- Files changed (6, installed via the staged zone-guard protocol; staging + proof harnesses retained in staging/pass4/ + tools/): hygiene/settings-cleanup.sh, post-compact-reminder.sh, scripts/cleanup-context.sh, audit/mutation-logger.sh, audit/write-mutation-logger.sh, quality/karpathy-surgical-diff-check.sh
- Levers: settings-cleanup >64KB path jq×6+grep×24+date/dirname/mkdir → jq×3 (analysis pass replicating the old render→gate→REPARSE chain incl. string-typed-allow fromjson promotion and ""/"null"/"[]" gates; UNCHANGED writer command = file bytes identical by construction; post-scan with whole-text test for \n-unmatchable patterns + line-model fallback for `[^`/backslash-letter/anchor patterns — profiled 17.7→2.3ms vs naive per-line any()); patterns stay in the ONE bash array, joined via printf -v (no spawn); audit_log date→printf %()T + [[ -d ]] mkdir guard. post-compact marker path cat×9+jq×4+date/dirname/mkdir → jq×1 (per-field try/catch keeps the old per-call default granularity) + heredocs kept byte-identical but consumed by builtin read -rd '' + $(<file) marker read + $(pwd)→$PWD (also removes a fork from the every-prompt path). cleanup-context ledger jq×2-3 → 1 (same selection/rendering; r() = string-raw/else-tojson) + Try-4 date → printf %()T; pass-4 note moved to file TAIL and line-count golfed so the --prompt /dev/tty bash diagnostic keeps `line 185` (golden untouched). mutation-logger stat probe BSD-first → GNU-first (GNU `stat -f%z` = "invalid option" rc=1, probed — a guaranteed wasted spawn per mutating call on Linux; isomorphic both platforms) + [[ -d .run ]] mkdir guard; wml same guard; karpathy SCRIPT_DIR dirname → pass-2 idiom + state-dir mkdir skipped only when [[ -d parent ]] PROVES dirname's target exists (no-slash/trailing-slash/missing-parent all fall back to old command) + TRAJ_DIR [[ -d ]] guard.
- DEFECT FOUND (old code, preserved for parity + logged in NOTES.md): the post-scan `grep -qP "$pat" || grep -qE "$pat"` passes patterns positionally without `--`, so the leading-dash '-----BEGIN .* PRIVATE KEY' pattern parses as OPTIONS → rc=2 silenced → that pattern could NEVER warn, GNU and BSD alike. Replicated explicitly (leading-dash → no-match) with a PRESERVED DEFECT comment; needs its own test-first /bug cycle.
- Spawn counts (census-pass4.tsv, pass3→pass4): settings-cleanup large 35→5 (grep×24 gone, jq 6→3, date/dirname/mkdir gone); post-compact marker 17→2 (jq×1+rm×1); ml mutating 5→3 (stat 2→1, mkdir gone); wml 2→1; karpathy warn 7→3 (mkdir×2+dirname×2 gone); cleanup-context ledger jq 2-3→1 (not in census matrix, A/B-timed). All pass-2/3 rows unchanged.
- Same-window A/B (ab-pass4.tsv, 20 runs, per-run ALTERNATION, mean ms): settings-cleanup large 90.3→59.4 (p95 101.1→65.7; remaining floor is the isomorphism-required 12-pattern filter ≈22ms — a single-alternation regex would contradict in-code decision BB-F7, left for a governance pass), small 6.5→6.1 (no regression); post-compact marker 39.7→14.2, no-marker 4.4→4.6 (wash ✓); ml mutating 15.5→13.0, benign 13.2→12.1; wml 8.9→7.0; karpathy warn 29.2→25.9; cleanup-context full-archive 39.7→37.2, no-ctx-dir wash.
- Proofs: 69-case differential corpus (tools/run-diff.sh; exit+stdout+stderr ts/root-normalized + FULL side-effect tree, NUL-safe walker for newline-bearing dirnames) = 0 divergences, 64 strict / 5 nostderr (out-of-contract allow types where old leaked jq/arith stderr noise — no-write parity + exit parity held); settings.local.json RAW sha256 byte-parity 25/25 sc-cases incl. the repo's REAL 55KB file (early-exit, untouched), real-padded >64KB rewrite, exact-65536 boundary, 65535, unicode/emoji, quotes/backslashes, malformed, empty, multi-doc, object/number/string-typed allow, 200/201 boundary, cred-in-deny warn path, cross-line `://…@` trap (whole-text vs line-model), privkey preserved-defect; host probes (GNU stat -f%z rc=1, printf %()T ≡ date ±0, $(<f) ≡ cat incl. missing-file/{}-fallback, per-field jq try ≡ 4-call granularity, grep↔jq hit-for-hit pattern parity); mirror-golden with staged overlay 32/32 BEFORE install; post-install golden 32/32 (zero goldens regenerated — line-golf kept bash diagnostics stable); bats 34 ok / 0 fail (hook-wiring ×9, agent-teams-hooks ×8, karpathy-surgical-diff-hook ×6, karpathy-trajectory-event ×4, karpathy-config-schema ×3, implementing-tasks-success-criteria-gate ×4; no bats coverage exists for the three rewritten scripts — differential corpus + goldens are the gate).
- Deliberate skips (with reasons): karpathy awk running-total NOT merged into the jq pass (awk's strtod-prefix textual parse counts lines_changed on crash-truncated/malformed state lines that fromjson can't replicate — kp-truncated-state-line case pins this); cleanup-context find|wc counting pairs kept (find -type f excludes symlinks, [[ -f ]] follows them — not isomorphic; rare path); mutation-logger mutating-filter `grep -qEi` kept (pass-2-class lever, out of this pass's mission; \s/\b portability trap noted); session-start hooks (loa-l6/l7, run-mode-session-title, loa-kf-surface) skipped — once-per-session (Impact≈1, Score<2.0) and L6/L7 are read-reference-first governed primitives; BB-F7 single-regex filter consolidation skipped (documented review decision).
- Artifacts: ab-pass4.tsv, census-pass4.tsv, staging/pass4/ (installed copies + tools/{run-diff.sh,ab.sh,capture-overlay.sh}), NOTES.md defect observation.
- Verdict: PRODUCTIVE

### Pass 5 — Boundary-script startup latency (beads-health, check-updates, golden-path) — 2026-07-05
- Files changed (5, installed via the staged protocol; staging + proof harness retained in staging/pass5/ + tools/): scripts/beads/beads-health.sh, scripts/check-updates.sh, scripts/golden-path.sh, scripts/path-lib.sh, scripts/bootstrap.sh (the latter two ARE golden-path's source-time cost — 58 scripts source bootstrap→path-lib, so the win compounds; laziness inside golden-path itself was rejected: GPG-T2 awk-extracts the top-level `_GP_GRIMOIRE_DIR=$(get_grimoire_dir)`…`fi` guard block verbatim, bug-980 requires loud source-time failure, and every consumer calls functions immediately after sourcing — so the init was made cheap instead of deferred).
- Where the time went (per-line EPOCHREALTIME xtrace + primitive timing): beads-health --quick ~50ms = br --version+head ~4, sqlite3 x3 ~5, awk x3+head ~5, stat BSD-fail x3 ~3, jq -R|jq -s ~6, date x2 ~2, dirname subshells ~1.5; full --json adds `br doctor` ~77ms (the floor — mission-untouchable). check-updates fresh-cache ~80ms = yq x5 ~19, jq-x4-on-cache ~8, jq version ~2, seq x2/cat/mkdir/date/dirname ~5. golden-path source ~85ms = path-lib config read (yq --version|grep|head ~6 + yq -e x5 ~28 + mktemp/rm ~5 + realpath x5 ~8) + compat-lib probes ~9 + bootstrap git/realpath/dirname ~4.
- Levers: (1) single-probe fast paths — `yq '.paths // ""'` / `.update_check // ""` == "" with rc 0 (probed: covers missing/null/empty-string key on yq 4.44.1) proves every per-key `.X // default` read returns its default, so 5 yq spawns collapse to 1 probe; ANY other probe outcome (map, scalar, parse error) falls through to the ORIGINAL per-key code verbatim — parity by construction for configured/broken configs; (2) check-updates cached-branch jq x4 → 1 jq with backslash-u0001 (SOH)-joined multi-field extract (NUL unusable: the $() capture must die on malformed cache exactly like old — same jq stderr, same rc 5; per-field tostring + trailing-newline strip + SOH scrub ≡ four $(jq -r) captures, and `printf '%s\n'` replicates echo's trailing newline byte-for-byte in jq's parse-error text); (3) _validate_paths realpath -m x4 → 1 batch call gated on all-4-nonempty-and-newline-free with ${VAR:-} guards (env-inherit branch leaves LOA_SOUL_* UNSET — an unguarded [[ under set -u would abort where old only lost a ||-true'd subshell), fallback per-path calls re-run at their ORIGINAL positions so leaked unbound-variable diagnostics interleave with validation errors in the old order; (4) yq-version grep|head → bash regex-match (leftmost match ≡ first-line-first-match); (5) pass-2/3/4 idioms: br-version head → first-line expansion (failure shape "head-output + newline + unknown" replicated), dirty_issues awk|head+awk x2 → one sqlite3 capture + bash field scan + IFS pipe-split read (field-2 match, field-4/5 extraction, no-pipe-line guard ≡ awk), stat BSD-first → GNU-first x3, date → printf time-format builtin x3, jq -R|jq -s → jq -nR '[inputs]' (probed identical), heredoc-cat → read -rd '', $(cat) → $(<file) (set -e death preserved via assignment), seq x2 banner rule → printf -v width-pad + space-to-rule expansion, mkdir -p → [[ -d ]] guard, dirname x4 → parameter expansion (no-slash/root edges).
- Same-window A/B (ab-pass5.tsv, 20 runs, per-run ALTERNATION, mean ms): bh --quick --json 48.9→38.0 (p95 65.5→49.5), bh --json 111.8→108.5 (br doctor ~77ms floor; overhead portion ~31→~24), check-updates --notify fresh-cache 81.6→29.9 (p95 103.1→37.5, −63%), source golden-path.sh 84.3→42.7 (p95 104.3→52.3, −49%; benefit repeats across all 58 bootstrap-sourcing scripts). Census (census-pass5.txt): bh-quick 22→9 spawns, cu-notify 17→4, gp-source 22→9.
- Proofs: 79-case differential corpus (tools/run-diff-pass5.sh; stdout+stderr+exit + cache-file bytes, ts/root-normalized) = 0 divergences — 21 beads-health (fixture schemas: healthy/no-owner/dirty-bug/nullable/no-table/no-db/corrupt/no-.beads/stale/missing-jsonl; br-absent PATH; LOA_BEADS_DIR override; per-side fixtures for the full-doctor case; real-repo --quick), 32 check-updates (fresh/empty/malformed/weird cache, style env+config, fetch success/prerelease/apierror/neterr/stale via curl PATH-shim + .venv-python-stub — ZERO network incl. DNS, config fastpath/keys/disabled/scalar/emptymap/malformed, TTL=0, CI/disabled env, --check/--help/badflag/no-version/non-tty), 26 golden-path (10 workflow states incl. bug+bridge+versionfile x full public-function battery, 9 config shapes incl. custom/scalar/empty-string/null/absolute/escape/malformed, env-inherit/legacy/state-dir rel+abs). Accepted divergence (documented, normalized): bash diagnostic LINE NUMBERS shift on two pre-existing stderr-leak paths (beads-health double-zero arithmetic error; path-lib unbound LOA_SOUL_* in env-inherit) — message text, ordering, rc all identical. jq container-valued cache fields would render compact-vs-pretty (pass-3-accepted class; the script-written cache cannot contain them). Post-install: staged==installed bytes x5, golden 32/32, bats 51 ok / 0 fail (beads-health-migration x5, check-updates x31, golden-path-grimoire-guard x5, bridge-golden-path x10), test-path-lib-state.sh 22/22, bridge-state + classify-commit-zone suites 0 fail, real-repo smoke (bh exit 4 DEGRADED, cu exit 1 banner, gp source+detect ok).
- DEFECT FOUND (old code, preserved for parity + logged in NOTES.md): check_schema missing-owner detection can never fire — `grep -c "owner" || echo "0"` yields a two-line "0 newline 0" on no-match (grep -c prints 0 AND exits 1), making the numeric [[ -eq 0 ]] an arithmetic syntax error that evaluates false → no-owner DBs report HEALTHY/compatible, exit 0. That grep line deliberately left untouched; needs its own test-first /bug cycle.
- Deliberate skips (with reasons): `br doctor` invocation (~77ms, the full-mode floor — behavior contract, mission-excluded); sqlite3 x3 merge into one call (corruption-path semantics interlock with the double-zero defect above — Score <2 at provable confidence); `grep -c owner` line (see defect — any touch changes defect bytes); compat-lib source-time probes ~9ms (probes ARE the correctness mechanism; per-call laziness touches 8 shared functions across a lib sourced by dozens of scripts — Score <2); check-updates network path (fetch/write_cache/stale-cache jq x4) untouched per mission; check-updates --json output_json cat-heredoc (not on the SessionStart hot path); endpoint-validator lazy-sourcing (parse cost probed ~1ms — not worth the failure-timing divergence); golden-path per-function jq/stat/md5 costs (call-time, not source-time — out of mission).
- Artifacts: ab-pass5.tsv, census-pass5.txt, staging/pass5/ (installed copies + tools/{run-diff-pass5.sh,ab-pass5.sh}), NOTES.md defect observation.
- Verdict: PRODUCTIVE

### Pass 6 — Memoization & caching of pure lookups across hook invocations — 2026-07-05
- Files changed (2, installed via the staged zone-guard protocol; staging + proof harness retained in staging/pass6/ + tools/): safety/zone-write-guard.sh, quality/karpathy-surgical-diff-check.sh. New State-Zone side effect (the ONLY one, documented in both hook headers): `.run/perf-cache/{zone-write-guard.v1.rows,karpathy-config.v1.tokens}` (gitignored; filename carries a version tag — any change to the memoized yq program MUST bump v1→v2 since the key deliberately excludes program text).
- Levers: the per-call yq derivations that are PURE functions of rarely-changing files — zwg's zone/TAB/pattern row stream (pure fn of zones.yaml) and karpathy's two-token config read (pure fn of .loa.config.yaml) — are memoized keyed on source identity: line 1 = `<ns-mtime>:<size>:<abs-path>` from ONE GNU-first `stat -Lc '%y:%s'` spawn (~1.4ms, replacing a ~4-7ms yq; non-GNU stat ⇒ key empty ⇒ never cache, permanent fail-open), rest = payload verbatim. %y nanosecond mtime rotates the key on same-second edits; the $PWD-absolutized path component blocks cross-repo/worktree contamination through shared relative LOA_ZONES_FILE/LOA_CONFIG_OVERRIDE names. Writes are atomic ($$-temp + `mv -f -T` same-dir; concurrent writers last-wins; readers never see torn files); reads use `IFS= read -rd '' 2>/dev/null <file || true` — NOT `$(<file)`, because a failed $(<file) redirection ABORTS an errexit shell even inside a `||` list (host-probed on bash 5.2; found when the corpus caught karpathy exiting 1 on an unreadable cache) — returning empty on missing/unreadable/dir/NUL-torn caches. ANY anomaly (key mismatch, stat failure, corrupt/truncated cache, race) falls through to the ORIGINAL parse verbatim and rewrites the cache; key is stat'd BEFORE the parse so a racing source write self-heals on the next call. Cold path in karpathy keeps the ORIGINAL procsub-mapfile byte-for-byte and caches the token ARRAY (newline-joined), so warm token arrays are provably identical (both tokens are single-line non-empty by construction of the yq program). `command -v yq` checks stay unconditional (yq-absent behavior unchanged); realpath and the stdin jq stay.
- Trust argument (zwg is a security fence): the cache adds NO new attacker capability — anyone who can write .run/perf-cache/ can already write grimoires/loa/zones.yaml (or .loa.config.yaml), the authoritative inputs these hooks trust today; both live in the same operator-writable State Zone, and forging the cache is strictly harder (must match the live file's stat identity). Same accepted-bypass posture as block-destructive-bash.sh per hooks-reference.md. The forged-key-match shape is pinned by two ACCEPTED-DIVERGENCE corpus cases asserting the documented behavior on both sides.
- Deliberately NOT cached (with reasons): adversarial-review-gate yq×2 — COMPLETED path fires ≤once/sprint (baseline "already fast" table), no shared helper exists (pass-5 lesson: sourcing costs; cache logic is inlined per hook) so it is not nearly-free ⇒ Score <2.0; team-role-guard-write `git rev-parse --show-toplevel` — ~0.96ms mean (pass-1 primitives), TEAM-mode-only path, and the value is a function of CWD/filesystem topology, not of any file whose identity can key a cache ⇒ no sound invalidation signal, Score ≈1.5 <2.0; ALL stdin-payload-derived values, .run state files (change per call), and timestamps — excluded by mission; spiral guards/mutation loggers have no pure file-derived parse left post-pass-3/4.
- Census (census-pass6.tsv): zwg write-grim/write-claude/edit-tests OLD 3 (yq,realpath,jq) → NEW-warm 3 (stat,realpath,jq — yq GONE), NEW-cold 6 (+stat,mkdir,mv once per zones.yaml change); kp warn OLD 3 (yq,jq,awk) → NEW-warm 3 (stat,jq,awk), NEW-cold 6. Same count, cheaper spawn: stat ≈1.4ms vs yq ≈4-7ms.
- Same-window A/B (ab-pass6.tsv, 20 runs, per-run ALTERNATION, mean ms): WARM zwg write-grim 24.3→17.2, write-claude 24.3→18.9, edit-tests 22.0→16.6; kp write-grim-warn 17.6→13.3, edit-tests-warn 20.8→16.4 (~10-11ms saved per Write/Edit across the two hooks). COLD (miss penalty, once per zones/config edit): zwg 23.1→28.8 (+5.7), kp 24.8→31.0 (+6.2) — amortizes after one warm call.
- Proofs: 45-case differential corpus (tools/run-diff-pass6.sh; per-STEP stdout+stderr+exit + full side-effect tree excluding only .run/perf-cache, ts/date/root-normalized) = 0 divergences — cache-state matrix per hook: cold→warm→warm sequences ×decision classes (framework/project/shared/unclassified ×actors ×env/arg/stdin target variants; warn/off/007/multidoc/empty-string/container/malformed configs), STALENESS (same-SIZE zones flip: rc 2→2→0→2; same-SECOND double rewrite: 2→0→2; kp threshold raise + switch-off mid-sequence — the edited source is honored on the VERY NEXT invocation), corrupt binary/truncated/empty caches, wrong-key forged rows/tokens ignored, cache-path-is-dir (kp set-e survival), unreadable cache (healed by atomic mv), read-only cache-dir and read-only .run, missing/malformed sources, multi-line + tab-bearing zone patterns round-trip, cross-contamination via shared relative source names under touch -d IDENTICAL mtimes+sizes (abs-path key component discriminates; semantic rc asserts), concurrent-writer stress (5×4 parallel cold starts, all decisions correct, cache intact), 2 forged-key ACCEPTED-DIVERGENCE cases (documented trust posture). Harness itself was debugged against vacuity: an unescaped sed alternation initially emptied the normalizer — fixed and re-proven (0 stderr lines; corpus caught 3 real defects during development: the $(<) errexit abort, a wrong xcontam target, the sed bug). Mirror-golden with staged overlay 32/32 BEFORE install (tools/capture-overlay-pass6.sh); atomic cp+mv install; staged==installed bytes ×2; post-install golden 32/32; bats 44 ok / 0 fail (zone-write-guard ×14, karpathy-surgical-diff-hook ×6, karpathy-trajectory-event ×4, karpathy-config-schema ×3, agent-teams-hooks ×8, hook-wiring ×9); real-repo smoke (live-session caches created with correct keys; BLOCK rc2 / ALLOW rc0 / kp rc0 warm).
- Accepted divergences (documented in headers, corpus-pinned): (1) key-matching forged cache is served — same trust domain as the source files themselves; (2) a NUL-bearing zones.yaml pattern warned "ignored null byte" on every call in the old $() capture, now only on the cold parse (out of contract, decisions unaffected); (3) trailing-EMPTY yq tokens would not round-trip the cache (unreachable by the token programs; downstream defaults coincide even if reached).
- Artifacts: ab-pass6.tsv, census-pass6.tsv, staging/pass6/ (installed copies + tools/{run-diff-pass6.sh,ab-pass6.sh,census-pass6.sh,capture-overlay-pass6.sh}).
- Verdict: PRODUCTIVE

### Pass 7 — Algorithmic wins on growing data: karpathy state O(n) → O(1)/O(delta) — 2026-07-05
- Files changed (1 hook, installed via the staged zone-guard protocol; staging + proof harness retained in staging/pass7/ + tools/): quality/karpathy-surgical-diff-check.sh. New State-Zone side effect: `.run/perf-cache/karpathy-state.v1.agg` (gitignored, version-tagged — bump v1→v2 if the warm/aux/builder jq programs or the awk fold change; full design block sits at the file TAIL, after `exit 0`, where bash never parses it).
- Profile-first: the per-call cost was O(session length) — the single jq `--rawfile`s ALL of karpathy-task-state.jsonl (files_modified set + tool_call_count) and awk rescans it for the running total. Old hook measured 20ms @ n=500 entries, 40 @ 5000, 111-118 @ 20000, 21-26 @ real n (639→664 during this pass — the file never rotates, so n grows with cycle age). Second profile finding that killed the v1 design: the REAL state holds 438 unique files across 646 entries (~32KB unique-file set) — set-heavy is the production shape.
- Design: aggregates (pois flag replicating the wholesale try/catch→0, newline count, mawk running total, unique-file set + count, prefix token) cached in a 9-line file keyed on dev:ino:abs-path with offset always on a '\n' boundary == last-write size. Per call ONE combined GNU-first stat (also serves the pass-6 config key, reassembled byte-identically — pass-6 caches stay valid, host-verified HIT). size==offset ∧ ns-mtime match ⇒ FAST (zero state reads; awk skipped — cached total is a bash-int product that round-trips mawk's double exactly, 15-digit guard); size>offset ⇒ DELTA: `tail -c +(offset-len(token))` procsubs token+delta into the one jq (token must match byte-for-byte) and THE SAME awk program seeded `-v init=` in BEGIN runs on a second tail procsub (`NR==1{next}`) — same left-to-right association as the full scan, mawk strtod-prefix semantics preserved by construction (same binary, same line bytes; '\n'-boundary offsets make line sets decompose exactly). ANY anomaly (size<offset, mtime drift at equal size, key/token mismatch, U+FFFD [jq's invalid-UTF-8 marker] in delta, corrupt cache/set, non-array set) ⇒ full rescan: cold-direct = ORIGINAL jq+awk verbatim; post-warm anomaly keeps the already-extracted payload fields (stdin consumed) + aux jq with the original state expressions PRE-append. Cache written only after a successful append, from a post-append stat, only when post-size == pre-size+delta+entry (concurrent appender ⇒ skip; stale cache stays consistent — next call processes a wider delta); cold writes rebuilt by a builder jq that re-reads post-append with FFFD/utf8len==size/ends-with-own-entry-line/all-strings guards (the glue case — pre-append unterminated final line — is computed correctly and simply not cached until the tail is clean again). Set-membership shortcut: `,"X",`-anchored substring on the compact set line proves membership without parsing (inside a compact jq string array a quote is always \"-escaped, so `,"` and `",` occur only at element boundaries); repeat-file calls (steady state) never parse, never re-unique, and pass the set line through an "=" sentinel — parse+unique only on a genuinely new file or delta-with-files. Size gate KARPATHY_STATE_CACHE_MIN (default 262144 ≈ the measured 1300-entry crossover): below it the ORIGINAL path runs end-to-end (census: 3 spawns = stat,jq,awk on BOTH sides — no post-stat, no mv, no cache) and offset≥gate for every written cache ⇒ no serve-without-write staleness loop; env override is harness-only (same trust class as KARPATHY_TASK_STATE).
- Mid-implementation defects caught by own proofs (fixed pre-install): jq `"" | split("\n")` is `[]` not `[""]` — the v1 nls fold under-counted tool_calls by 1 per empty delta (12-step smoke caught byte drift in trajectory `tool_call_count`); v1 shuttled the 32KB set through argv+fromjson+unique EVERY call and a 25KB herestring mapfile costs 3.7ms — v1 was SLOWER than old at n≤650 (per-run-alternated A/B caught it; redesigned to line-reads from one fd + membership shortcut + set-last format); a group-redirect's `2>/dev/null` must precede `< file` or the open-failure diagnostic leaks (corpus case seq-cache-patho caught the unreadable-cache stderr leak); bash-lexing-cost hypothesis for the residual +4ms DISPROVEN by measurement (47KB parses in <1ms — spawn floor dominates; the real cost was the ungated post-stat, moved behind the S0-based gate).
- Same-window A/B (ab-pass7.tsv, 20 runs, per-run ALTERNATION, warn regime, mean ms; default gate): n=500 18.6-20.5→20.1-24.2 (below gate — original path, wash within governor noise), n=5000 42.9→18.5 (p95 53→24), n=20000 118.1→18.0 (p95 130→24, 6.6×), real n=664 21.3→23.0 (below gate today; crossover ≈1300 entries ≈ days of sessions, after which old grows ~4.7ms/1000 entries unbounded and new stays flat ~18), delta scenario (external append between calls, n=5000) 43.9→26.1, cold miss @500 20.6→23.1. Forced-warm floor rows recorded (gate=0: ~27-31 flat at any n). Census (census-pass7.tsv): below-gate 3→3 spawns IDENTICAL shape (stat,jq,awk — pre-stat rides the combined config stat); warm-fast 3→4 (awk gone, +post-stat+mv, jq no longer scans state); warm-delta 3→7 (+tail×2); cold-above-gate 3→6 (builder, once per anomaly).
- Proofs: 32-case differential SEQUENCE corpus (tools/run-diff-pass7.sh; per-STEP stdout/stderr/exit ts/root/line-number-normalized + final side-effect tree excluding only .run/perf-cache + a cache-consistency INVARIANT check after every case: cache lines must equal a fresh full-scan recomputation over the first `offset` bytes incl. count==|set| and token==last-line and '\n'-boundary) = 0 divergences: 14-step warn-regime growth (WARN-line byte-parity at every step), no-warn/mixed-tools/junk-stdin, truncation, rotation (new inode), same-size in-place rewrite, external appends (delta), malformed-delta strtod battery (truncated-JSON digit prefix, ` 42,`-space, `:}`-empty, whitespace-only, `null`, non-string .file), non-integral running-total → identical bash arithmetic death both sides (line numbers normalized, pass-5-accepted class), glue (unterminated final line incl. valid-JSON partial — self-heals), unicode payloads/multibyte tokens, NUL lines, invalid-UTF-8 delta (FFFD→anomaly→aux), state deleted/unreadable mid-sequence, empty-start, corrupt/truncated/extra-line/bad-numeric/bad-set caches, cache-is-dir/unreadable-cache/RO-perf-cache/RO-.run, cross-file contamination via alternating KARPATHY_TASK_STATE, leading-zero threshold 007 warm, USER-derived session id, oversized set (>64KB ⇒ permanent full-scan, cache absent), >1024B token ⇒ write skipped, EVAL_ERR payloads exit 5 under warm cache, relative state path, missing .run, gate-default (no cache below gate) + gate-cross (caching starts above); plus 2 ACCEPTED-DIVERGENCE assertions (key-matching forged cache IS served — forged sum 999 → warn total 1002 also proves fast-path non-vacuity; touch -d mtime-forgery serves stale set — same pass-6 trust posture: cache writers can already write the state file itself) and a 4×5 concurrent-append stress (line count exact, cache invariant intact, next sequential total == full-scan 46). Harness vacuity-guarded (normalizer self-test; the first smoke's sed `|`-delimiter bug made diffs vacuous and was caught+fixed — pass-6 lesson repeated). Host probes: jq --rawfile on procsub works; invalid UTF-8 → U+FFFD replacement (detectable, length-preserving cases included); NUL preserved (utf8bytelength counts it); mawk (not gawk!) prints ≤15-digit integrals exactly, `-v init` BEGIN-seeding preserves association, `0x`/`inf` do NOT parse (documented, not replicated — same binary reused). Mirror-golden with staged overlay 32/32 BEFORE each install (tools/capture-overlay-pass7.sh); atomic cp+mv installs; staged==installed bytes; post-install golden 32/32; bats 30 ok / 0 fail (karpathy-surgical-diff-hook ×6, karpathy-trajectory-event ×4, karpathy-config-schema ×3, hook-wiring ×9, agent-teams-hooks ×8).
- Accepted divergences (documented in the tail design block, corpus-pinned): key+token-matching forged cache served (trust domain); /dev/fd procsub-plumbing failure would no-op one call (indistinguishable from invalid stdin; not reproducible on Linux; warn-only hook); read-instant races within the old code's own two-read race envelope; harness-only gate-env mixing against one state file is unsupported.
- Secondary targets surveyed and SKIPPED with evidence: memory-query.sh — referenced ONLY by itself and its test (grep across hooks/skills/settings), ad-hoc CLI not a hot path (Impact≈0); mutation-logger rotation `tail -n 1000` — fires only >10MB (audit.jsonl at 7.3MB today, ≈once per ~50K mutating calls; the per-call cost is the stat already GNU-first from pass-4) — Score≪2; loa-kf-surface.sh — SessionStart once, single awk pass over known-failures.md (Impact≈1, baseline "already fast" class); memory-writer.sh — not wired in settings.json, observations.jsonl is 1 line.
- Artifacts: ab-pass7.tsv, census-pass7.tsv, staging/pass7/ (installed copy + tools/{run-diff-pass7.sh,ab-pass7.sh,capture-overlay-pass7.sh}).
- Verdict: PRODUCTIVE
