# Fake project instructions (correlator bats fixture)

This file exists only so the fixture's declared `inputs:` manifest entry
`CLAUDE.md` resolves to a real path under `fake-repo/`. Content is never
inspected by the correlator — only the path is compared.
