# Envio API token — https://envio.dev/app/api-tokens
ENVIO_API_TOKEN="<YOUR-API-TOKEN>"

# Which config the belt starts with. Defaults to config.yaml.
# BELT_CONFIG=config.yaml

# Set to 1 to start the indexer from scratch (drops indexed state and re-syncs).
# ENVIO_RESTART=0
