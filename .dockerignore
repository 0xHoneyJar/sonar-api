# Slim the Railway/Docker build context (applies to Dockerfile.belt + Dockerfile.erpc).
# Build outputs are regenerated by `envio codegen` inside the image.
node_modules
generated
.envio
envio-env.d.ts
.git
.run
.beads
.ck
grimoires
*.log
.DS_Store
# Never bake local env files into the image (COPY . . would otherwise include them).
.env
.env.*
