# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
#
# GENERATED FILE — do not edit by hand.
# Source of truth: src/registry/contracts.ts. Regenerate with `pnpm gen:config`.
# test/contract-registry.test.ts fails if this file drifts from the registry.
name: thj-indexer
contracts:
  # Every tracked ERC-721. One handler, one binding, no per-community case.
  - name: TrackedErc721
    handler: src/EventHandlers.ts
    events:
      - event: Transfer(address indexed from, address indexed to, uint256 indexed tokenId)
        field_selection:
          transaction_fields:
            - hash
            - transactionIndex
  # Every tracked ERC-1155. Balances are per (contract, chain, tokenId, wallet);
  # TransferBatch is folded into the same path as TransferSingle.
  - name: TrackedErc1155
    handler: src/EventHandlers.ts
    events:
      - event: TransferSingle(address indexed operator, address indexed from, address indexed to, uint256 id, uint256 value)
        field_selection:
          transaction_fields:
            - hash
            - transactionIndex
      - event: TransferBatch(address indexed operator, address indexed from, address indexed to, uint256[] ids, uint256[] values)
        field_selection:
          transaction_fields:
            - hash
            - transactionIndex
  # Every tracked ERC-20. Same topic0 as ERC-721's Transfer — the third arg is
  # un-indexed here, so a contract registered under the wrong standard decodes garbage.
  - name: TrackedErc20
    handler: src/EventHandlers.ts
    events:
      - event: Transfer(address indexed from, address indexed to, uint256 value)
        field_selection:
          transaction_fields:
            - hash
            - transactionIndex
  # Seaport (OpenSea) — secondary sales. The OrderFulfilled ABI is stable across
  # versions, so one binding covers v1.1 through v1.6 on every chain.
  - name: Seaport
    handler: src/EventHandlers.ts
    events:
      - event: OrderFulfilled(bytes32 orderHash, address indexed offerer, address indexed zone, address recipient, (uint8,address,uint256,uint256)[] offer, (uint8,address,uint256,uint256,address)[] consideration)
        field_selection:
          transaction_fields:
            - hash
  # Blur v1 — Ethereum only, near-dormant now but carries Blur's 2022-23 volume.
  # One matched pair is one NFT; the order carries its own price and currency.
  - name: Blur
    handler: src/EventHandlers.ts
    events:
      - event: OrdersMatched(address indexed maker, address indexed taker, (address,uint8,address,address,uint256,uint256,address,uint256,uint256,uint256,(uint16,address)[],uint256,bytes) sell, bytes32 sellHash, (address,uint8,address,address,uint256,uint256,address,uint256,uint256,uint256,(uint16,address)[],uint256,bytes) buy, bytes32 buyHash)
        field_selection:
          transaction_fields:
            - hash
  # Blur v2 — where all current Blur volume is. Fields are BIT-PACKED into two
  # uint256s and only ONE party is on the log, so `from` is selected: the
  # counterparty is the transaction sender (verified against 16 real fills).
  - name: BlurV2
    handler: src/EventHandlers.ts
    events:
      - event: Execution721Packed(bytes32 orderHash, uint256 tokenIdListingIndexTrader, uint256 collectionPriceSide)
        field_selection:
          transaction_fields:
            - hash
            - from
chains:

  # Berachain Mainnet
  - id: 80094
    start_block: 233064
    hypersync_config:
      url: https://berachain.hypersync.xyz
    contracts:
      - name: TrackedErc721
        address:
          - "0x6666397dfe9a8c469bf65dc744cb1c733416c420" # mibera_collection
          - "0x1790b94e9394f817b3161d8f883317fcca233dfa" # beraborrow
          - "0xcb86b75ee6133d179a12d550b09fb3cdb1e141d4" # dolomite
        start_block: 811704
      - name: TrackedErc20
        address:
          - "0xc0d1ac00a30fa4e30e44afc7313d6312c87e21df" # kodiak
          - "0x040ea7d4b559357425407fdfc3c774c5dfc04677" # kodiak
          - "0xb7e448e5677d212b8c8da7d6312e8afc49800466" # goldilocks
          - "0xbf2e152f460090ace91a456e3dee5acf703f27ad" # goldilocks
          - "0x1ce0a25d13ce4d52071ae7e02cf1f6606f4c79d3" # beraborrow
          - "0x597877ccf65be938bd214c4c46907669e3e62128" # beraborrow
          - "0xc99e948e9d183848a6c4f5e6c1d225f02f171d79" # beraborrow
          - "0x0f81001ef0a83ecce5ccebf63eb302c70a39a654" # dolomite
          - "0xac03caba51e17c86c921e1f6cbfbdc91f8bb2e6b" # infrared
          - "0x9b6761bf2397bb5a6624a856cc84a3a14dcd3fe5" # infrared
          - "0xa1b644aec990ad6023811ced36e6a2d6d128c7c9" # infrared
        start_block: 233064
      - name: Seaport
        address:
          - "0x0000000000000068f116a894984e2db1123eb395" # Seaport v1.6
        start_block: 811704
